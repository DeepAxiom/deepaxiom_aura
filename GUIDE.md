# Deep Axiom — a streaming runtime for AI skills

**Status: v0.3.0 — pre-1.0, pre-production, Phase 1 of 2 toward open beta.** · **Built on the AURA kernel architecture · [Apache-2.0 (spec & SDK) · AGPLv3 or commercial (kernel) — see LICENSE.md](LICENSE.md)** · [Versión en español](GUIDE-ES.md) · [← README](README.md)

Deep Axiom is an open-source runtime for composing skills — LLMs, speech,
databases, business APIs — into graphs that run as live, typed streams. One
binary holds the kernel, the control-plane UI, the state store, the message
bus and the effect ledger; it needs no account, no cloud, and no external
database. Graphs can be written by hand or generated by a planner from a
natural-language goal, and every session keeps an append-only causal event
log you can explain or replay afterwards.

Four design choices distinguish it from workflow tools (n8n, Zapier, Make)
and from agent frameworks:

- **The connection, not the run, is the unit of work.** Messages are envelopes
  on a typed, ordered, causal, back-pressured stream over a socket meant to
  stay open — not steps in a batch that spins up, runs and tears down.
- **Two safety properties live in the kernel rather than in userland.** Every
  edge into a skill that acts on the world (`motor.*`) carries a gate the
  node's own policy decided on — not the graph's author — applied by the
  executor so a hand-written graph cannot skip it; and a cancelled chain is
  suppressed by the kernel, so a skill that ignores the cancel produces output
  that goes nowhere.
- **Every effect is sealed into a hash-chained, signed ledger — not logged,
  attested.** A node authorizes an effect by policy, records it in a chain
  where altering an old entry breaks every hash after it, commits to an
  RFC 6962 Merkle head, and signs that head. A third-party **witness** can
  counter-sign it, which is what rules out the node's own operator rewriting
  history. Any single effect can be exported as a **portable receipt** anyone
  verifies offline — no database, no node, no network.
- **The ledger records what *argued* for an act, not only who authorized it.**
  A skill that runs a model attests to it — engine, model, Hugging Face
  revision, quantization, sampling parameters, seed — and the kernel binds
  that record into every effect the output causally led to. So a sealed entry
  answers "on what basis did this happen", and a silent model swap changes
  hashes already committed to an append-only chain. It is an *assertion* bound
  unforgeably to its consequences, not proof of what executed; see
  [Security model](#security-model), which says so at length.

**What this is not, yet.** There is no multi-device view of one live session,
and no failover if the owning node dies — a session's state survives a dropped
socket, but not the node process disappearing. A node authenticates — loopback
by default, a bearer token, a WebSocket origin check, optional TLS — but every
caller shares one node token, so a compromised skill holds the same credential
the operator does. And a `format: source` skill is not contained: `--sandbox
process` scrubs its environment and jails its working directory, `format: wasm`
is genuinely sandboxed, but neither contains hostile code. Read
[Security model](#security-model) before you expose a port;
[Milestone status](#milestone-status) and
[Designed, not yet built](#designed-not-yet-built) list all of this explicitly.

To add a capability, write a skill and register it — there is no review queue,
because the registry is yours to host.

**The skills in [`skills/`](skills/) are demos, not a catalogue.** They exist to
show the shape of a skill and to give a cold node something to run; every one of
them declares the `example/` org for that reason. Read them as the reference
implementation of C1 and the channel protocol, copy the closest one, and replace
it — [`skills/echo/`](skills/echo/) is the minimal skill,
[`skills/postgres-cdc/`](skills/postgres-cdc/) a `sensorial` skill wrapping an
external system, [`skills/model-manager/`](skills/model-manager/) a `motor` skill
that acts on the world. None of them carries the retry policy, credential
rotation or schema-change handling a production deployment needs, because those
are decisions a deployment makes and an example cannot make for it. See
[`CONTRIBUTING.md`](CONTRIBUTING.md) for the kernel/SDK/spec themselves.

---

## Table of contents

1. [What problem it solves](#what-problem-it-solves)
2. [How it compares](#how-it-compares)
3. [Feature highlights](#feature-highlights)
4. [Use cases](#use-cases)
5. [Core concepts](#core-concepts)
6. [Architecture](#architecture)
7. [Install & quickstart](#install--quickstart)
8. [Command reference](#command-reference)
9. [Worked examples](#worked-examples)
10. [The control plane UI](#the-control-plane-ui)
11. [Writing a skill (the SDK)](#writing-a-skill-the-sdk)
12. [Runtime skill config](#runtime-skill-config)
13. [Connecting existing software](#connecting-existing-software)
14. [Talking to it](#talking-to-it)
15. [Natural-language operation](#natural-language-operation)
16. [Typed ports, enforced](#typed-ports-enforced)
17. [Scheduling: speculation, deadlines, budgets](#scheduling-speculation-deadlines-budgets)
18. [Model drivers & resource admission](#model-drivers--resource-admission)
19. [Audit bundles](#audit-bundles)
20. [An OpenEnv environment](#an-openenv-environment)
21. [Signed approval — who allowed it](#signed-approval--who-allowed-it)
22. [The credential broker](#the-credential-broker)
23. [Regression testing against the ledger](#regression-testing-against-the-ledger)
24. [Skill isolation](#skill-isolation)
25. [Open witnessing](#open-witnessing)
26. [Who watches the witness](#who-watches-the-witness)
27. [The marketplace](#the-marketplace)
28. [Explainability & replay](#explainability--replay)
29. [Federating nodes](#federating-nodes)
30. [Standards at the borders](#standards-at-the-borders)
31. [Guarding an agent's tools](#guarding-an-agents-tools)
32. [The five contracts](#the-five-contracts)
33. [Security model](#security-model)
34. [HTTP & WebSocket API](#http--websocket-api)
35. [Repository layout](#repository-layout)
36. [Build & release](#build--release)
37. [Milestone status](#milestone-status)
38. [Designed, not yet built](#designed-not-yet-built)
39. [License & governance](#license--governance)

---

## What problem it solves

Workflow automation platforms — n8n, Zapier, Make — solved "connect my existing
tools" for *batch* work: a trigger fires, a chain of steps runs once, the run is
done. That fits a nightly sync and a webhook. It fits a voice conversation or a
long-lived multi-agent session badly, because there the state that matters lives
between messages rather than inside one run.

Making the connection the unit of work instead is not a new idea — chat and game
infrastructure has worked this way for years, and Temporal and the real-time
voice frameworks each solve a piece of it. What is unusual here is applying it to
*legacy-connected* AI work: a runtime installed next to existing systems, talking
to them over the same streaming protocol it uses for models and clients. Three
consequences follow that the rest of this guide is largely about:

- **Legacy-first.** The first useful command is *connect what you already have*,
  not "create a project". Point it at an OpenAPI spec and its operations become
  skills — read-only by default, with human approval required before anything
  writes. See [Connecting existing software](#connecting-existing-software).
- **One interface for every model.** LLMs, ASR, TTS — and any model a skill
  wraps — all behind the same skill contract, local or remote. Moving a skill to
  another machine changes placement, not the graph. See
  [Model drivers](#model-drivers--resource-admission).
- **Federable distribution.** The spec, kernel and SDKs are open and the
  registry is federable, so the catalog you install from is one you can host.
  See [The marketplace](#the-marketplace).

---

## How it compares

Most of what this runtime does, something else already does — usually with more
maturity, more integrations, or both. An honest map of where it sits:

| If you need | Use | Deep Axiom's position |
|---|---|---|
| Hundreds of ready SaaS integrations | **n8n, Zapier, Make** | Not competitive *on catalogue*. Theirs is the product; the nine skills here are [worked examples](skills/), not an inventory — the integration story is `aura connect` (any OpenAPI spec becomes skills) and `aura guard` (any MCP server), not a shelf we stock. |
| Durable, replayable long-running workflows | **Temporal** | Not competitive on durability. A dropped session *does* resume — the dedup window, causal indexes, pending gates and per-hop counters rebuild from the event log whether the client or the kernel was what died — but there is still no failover to a second node, which is Temporal's whole point. What this adds instead is a signed, hash-chained attestation of every effect, verifiable offline. |
| Production real-time voice agents | **LiveKit Agents, Pipecat, OpenAI Realtime** | Far less mature. First sound of a reply lands at ~0.41s on one developer machine with a local model — never benchmarked against these side by side, so read it as "usable", not "competitive". Choose those unless you need voice on the *same* runtime as the rest. |
| An agent library inside your own app | **LangGraph, CrewAI, AutoGen** | Different shape. Those are libraries you build with; this is a process you install beside existing systems, and the interrupt they'd have you code by hand is a kernel invariant here. |
| Tool discovery for a model | **MCP** | Not a competitor — this ships an MCP server so its skills are MCP tools, and an effect an MCP-connected agent triggers is sealed the same as any other. |
| A safety layer over the agent you already run | **nothing that ships today** | `aura guard` fronts an agent's existing MCP servers so every tool call is authorized by node policy, gated if it acts on the world, and sealed into a verifiable ledger — without rewriting the agent. Audit-log products sit *beside* a runtime and record; this sits *in front of* the call and can refuse it. The catch is real and stated up front: it holds only as far as you control the agent's config. |

What is genuinely different, and the reason to look at this rather than the
above:

- **Every effect is authorized, attested, reversible, and traceable to the
  model that argued for it; nothing else combines all four.** A node's policy
  decides whether an effect needs approval (not the graph that requests it),
  the decision and its outcome are sealed into a hash-chained, Merkle-committed
  ledger the node signs and a third party can counter-sign, `aura undo`
  reverses it through a declared compensation port, and the entry cites the
  model revision and sampling parameters behind it. Temporal has durable
  execution with no policy or attestation layer; LangGraph's interrupt lives
  in application code; n8n has an approval node and logs; model-signing and
  TEE-attestation projects secure weights and enclaves but are not attached to
  a runtime that records effects. Nobody ships the combination as a kernel
  guarantee. See [Security model](#security-model).
- **The human-approval gate is a kernel invariant, not graph code.** In agent
  libraries the interrupt lives in the graph you wrote, so a graph that forgets
  it has no gate. Here the executor applies it at the one point every graph
  passes through, driven by node policy — a graph may ask for *more* scrutiny
  than the policy requires, never less — and `published` mode *refuses* an
  ungated `motor.*` edge rather than silently repairing it.
- **Cancellation is a guarantee, not a request.** The kernel suppresses
  everything belonging to a cancelled chain, so a skill that ignores the cancel
  cannot deliver anyway — and it addresses each skill with the `cause_id` *that
  skill* will recognise, at any depth.
- **One IR for hand-written and planner-generated graphs.** One executor, one
  permission model, one replay path, one debugger — rather than a separate
  path for "the agent decided this".
- **One binary, no external services.** ~22 MB with the UI embedded, and no
  Postgres, no Redis, no broker, no cluster to stand up before the first
  message — the ledger lives in the same embedded SQLite file as everything
  else.

The contracts these rest on are frozen and specified in [`spec/`](spec/), with a
conformance suite an implementation uses to demonstrate it complies.

---

## Feature highlights

A map, not a summary — each line names the section that explains the thing
properly, because a feature list that restates thirty sections is a second copy
of the guide that drifts out of step with the first.

| | |
|---|---|
| **One binary** — kernel, UI, state store and message bus, no external services | [Architecture](#architecture) |
| **Streams as the primitive** — typed, ordered, causal, idempotent, back-pressured channels carrying text, audio, documents and events alike | [Core concepts](#core-concepts), [Typed ports](#typed-ports-enforced) |
| **One graph format** for hand-written and planner-generated graphs — one debugger, one permission model, one replay path | [Natural-language operation](#natural-language-operation) |
| **Any model behind one interface** — LLM, ASR, TTS, local or remote, swappable without touching the graph; plus a memory budget the node admits against | [Model drivers & resource admission](#model-drivers--resource-admission) |
| **Tunable skills at runtime** — manifest defaults, a `--config` file, live overrides hot-applied over the skill's own connection | [Runtime skill config](#runtime-skill-config) |
| **Legacy-first** — an OpenAPI spec becomes skills, read-only by default, with dry-run and per-operation promotion; IoC skills dial out through NAT | [Connecting existing software](#connecting-existing-software) |
| **Standard borders** — an MCP server whose `tools/call` streams, an A2A card, OpenTelemetry export | [Standards at the borders](#standards-at-the-borders) |
| **`aura guard`** — an agent's existing MCP servers put behind this node's policy, gate and ledger, with no rewrite | [Guarding an agent's tools](#guarding-an-agents-tools) |
| **The gate as a kernel invariant** — the executor applies it, so a graph that forgets it still has one | [Authorization](#authorization--the-node-decides-not-the-graph) |
| **A skill is not the operator** — a credential scoped to one capability, which cannot register a graph or read the ledger | [Scoped credentials](#scoped-credentials) |
| **Who approved it, signed** — an enrolled operator's signature sealed into the entry, made with a key the node has never held | [Signed approval](#signed-approval--who-allowed-it) |
| **A credential only against a receipt** — the secret lives in the kernel and is released against a sealed effect, so bypassing the gate gets a 401 | [The credential broker](#the-credential-broker) |
| **The effect ledger** — hash-chained, Merkle-committed, signed, verifiable offline from the database file with no kernel running; `aura undo` reverses through a declared compensation port | [Attestation](#attestation--the-effect-ledger-c4) |
| **Portable receipts and audit bundles** — one effect's or one session's evidence, verifiable by anyone, disclosing nothing else | [Audit bundles](#audit-bundles) |
| **Attested inference** — engine, model, revision, quantization, sampling and seed bound into every effect that output caused | [Attestation of the inference itself](#attestation-of-the-inference-itself-c5) |
| **Accountable third parties** — anchor at a witness that publishes its own log and signs how far it has vouched | [Open witnessing](#open-witnessing), [Who watches the witness](#who-watches-the-witness) |
| **Effect-level regression** — replay recorded sessions against a new model and diff the acts, not the transcripts | [Regression testing](#regression-testing-against-the-ledger) |
| **Explain and replay** — `aura why` narrates a failure from the causal log | [Explainability & replay](#explainability--replay) |
| **Signed, federable marketplace and node federation** | [The marketplace](#the-marketplace), [Federating nodes](#federating-nodes) |

Everything above exists and runs. What is **not** there — process isolation for
`format: source` skills, `site` mode, multi-device sessions, node failover — is
listed just as explicitly in [Security model](#security-model) and
[Milestone status](#milestone-status), along with which parts a regression would
actually catch.

---

## Use cases

Concrete shapes the same runtime takes on. In every one, the AI is added *onto*
what exists, not instead of it.

- **AI over a legacy ERP/CRM.** `aura connect` an internal OpenAPI, and staff
  operate the system in natural language — "invoice this order, then email the
  customer" — with every write requiring a human OK. No changes to the ERP.
- **Real-time voice assistant.** Compose `sensorial.asr.transcribe` →
  `cognitive.llm.chat` → `motor.tts.speak` into a graph. Speech in, reasoning,
  speech out, streaming — the pieces are interchangeable skills.
- **Change-data-capture into reasoning.** `skills/postgres-cdc` turns a legacy
  database's own replication stream into causal events; an LLM classifies each
  row change; a projected API files the result. Sensitive data can be pinned to
  on-premise nodes so it never leaves the building.
- **Edge + cloud split.** Run capture and privacy-sensitive inference on an edge
  node (a Jetson on the factory floor); federate the heavy reasoning to a cloud
  node. One logical graph, placement decided per skill; the site keeps working if
  the link drops.
- **Multi-agent back office.** Specialist skills — a researcher, a writer, an
  approver — chained with human gates between them, and `aura why` explains
  any run that went sideways.
- **Interop hub for existing AI tools.** Expose your organization's skills as MCP
  tools so Claude Code / Cursor / any MCP client can call them, and export every
  session to your existing OpenTelemetry stack for observability.
- **A skills marketplace.** A team publishes a signed, versioned skill (a domain
  OCR, a compliance checker); other teams install it by capability and run it in
  their own nodes, or call it remotely — the registry you host, not a vendor's.
- **Developer platform.** Ship Deep Axiom embedded in your product so *your* customers
  add AI to *their* stack, with your own federated registry as the catalog.

---

## Core concepts

You only need the first two on day one. The rest are discovered when they hurt.

| Concept | What it is |
|---|---|
| **Skill** | The atomic unit of function — something the system *knows how to do*. It has an identity, typed ports, and a manifest. A skill can be logic, a model, or a projection of an existing system. |
| **Channel** | A typed stream between two ports. Ordered (FIFO), causal (every message names its cause), idempotent (at-least-once with dedup keys), back-pressured, and meterable. |
| **Graph** | Skills wired together by channels. Written by a human in a few lines, or generated by a planner at runtime — both compile to the *same* intermediate representation and run through the *same* executor. |
| **Kernel** | A single binary with four primitives: identity, channels, a live skill registry, and a graph executor. Embedded SQLite for state, embedded message bus, zero external services. |
| **Node** | A kernel process on one machine. Nodes run standalone and can federate. |
| **Session** | A live instance of a graph, owned by exactly one node, with its own append-only causal event log. |
| **Projection** | An existing system (an API, a database, an MCP server) exposed *as skills* without changing its code. |
| **Registry** | A federable package host where skills are published (signed) and installed, resolvable by capability. |

### The five skill types

Every skill declares one type, which also prefixes its capability
(`sensorial.asr.transcribe`, `motor.tts.speak`, …). The type is not cosmetic:
the runtime uses it for policy — for example, edges into `motor.*` skills always
get a human-approval gate in planner- and MCP-generated graphs.

| Type | Role | Examples |
|---|---|---|
| `sensorial` | Perceives — turns the world into data | ASR, OCR, cameras, file readers |
| `cognitive` | Reasons — decides, plans, generates | LLM chat, planner, classifiers |
| `motor` | Acts — produces effects in the world | send a message, write to an ERP, speak |
| `memory` | Remembers — persists and retrieves context | history, semantic memory |
| `logical` | Transforms/validates — deterministic data → data | parsers, validators, format bridges |

---

## Architecture

Three planes, cleanly separated, so the parts that change fast never destabilize
the parts that must stay stable for years.

```
╔══════════════ SPECIFICATION (neutral, versioned, conformance-tested) ═══════════╗
║   C1 Manifest          C2 Graph IR           C3 Channel Protocol                ║
╚═════════════════════════════════════════════════════════════════════════════════╝
                                    ▲ implemented by
┌────────────────────────────── KERNEL (one binary) ──────────────────────────────┐
│  Identity   ·   Channels   ·   Skill registry   ·   Graph executor              │
│  embedded SQLite (state + causal event log)   ·   embedded bus   ·   admission  │
└──────────────────────────────────────────────────────────────────────────────────┘
                                    ▲ everything below is USERLAND (replaceable)
┌──────────────────┬──────────────────┬──────────────────┬───────────────────────┐
│ COGNITION        │ MODEL DRIVERS    │ PROJECTIONS      │ STANDARD BORDERS      │
│ planner, chat,   │ llama.cpp, ONNX, │ OpenAPI → skills │ MCP server, A2A card, │
│ memory           │ whisper, TTS     │ (legacy-first)   │ OpenTelemetry export  │
└──────────────────┴──────────────────┴──────────────────┴───────────────────────┘
                                    ▲ distributed via
┌───────────────── FEDERABLE REGISTRY ──────────────┐   ┌──────── FEDERATION ─────┐
│ local (~/.aura) → site → public, one protocol     │   │ node ⇄ node, leaf-node  │
└───────────────────────────────────────────────────┘   └─────────────────────────┘
```

**The load-bearing idea:** the kernel knows nothing about LLMs, models, APIs, or
the marketplace. Cognition (the planner, chat), the model drivers, the legacy
projections, the MCP server, and even federation are all *userland* — they
connect to the kernel over the same public protocol any third party uses, with no
special privileges. The purity test: if a feature needs an LLM to work, it does
not belong in the kernel.

- **Control plane** — the kernel: who exists, what they offer, who may talk to
  whom, where work runs. It signals and relays; it does not embed intelligence.
- **Data plane** — the channels: typed, causal streams between skills. Today they
  relay through the kernel; the protocol already carries what negotiated
  peer-to-peer transport (LAN, QUIC/WebRTC) will need.
- **Model plane** — the drivers: native inference (llama.cpp, ONNX Runtime,
  whisper, OS voices) behind one uniform skill interface. A model is just a skill.

---

## Install & quickstart

**Requirements:** Go 1.25+ to build the kernel; Python 3.11+ to run the
first-party skills; Node 20+ only if you want to rebuild the UI. Consumer
hardware (8 GB RAM) is enough.

```powershell
# 1. Build the kernel (the UI ships embedded — no Node needed at runtime)
cd kernel
go build -o aura.exe ./cmd/aura
.\aura.exe up
#   → binds 127.0.0.1 and prints a tokenized UI URL:
#       http://localhost:9080/#token=<generated once, stored in <data-dir>\node.token>
#   → MCP endpoint at http://localhost:9080/mcp
```

`aura up` starts a fully working node — kernel, UI, and four graphs seeded out
of the box (`echo`, `chat`, `plan`, and `voice`). Open the URL it prints: the
token rides in the fragment, which browsers never send to a server, and the page
stores it for the tab and strips it from the address bar. Other `aura` commands
read the same token from the data dir, so nothing else needs configuring.

In a second terminal, prove the whole path works with no model and no downloads
at all:

```powershell
# 2. The smallest possible skill — no model, no credentials, no network
pip install -r skills\echo\requirements.txt
$env:PYTHONPATH="sdk\python\src"
cd skills\echo; python main.py

# then, from the repo root:
.\kernel\aura.exe chat --graph echo "test"    # → test
```

Then bring a local LLM online and talk to it for real:

```powershell
# 3. The local LLM skill (from the repo root)
pip install -r skills\llm-chat\requirements.txt
#   llama-cpp-python CPU wheel:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
cd skills\llm-chat
$env:PYTHONPATH="..\..\sdk\python\src"; python main.py
#   downloads Qwen2.5-1.5B (~1.1 GB) on the first message
```

```powershell
.\kernel\aura.exe chat            # interactive REPL
.\kernel\aura.exe chat "hello"    # one-shot
```

> **Flags go before the message.** `aura chat --graph echo "test"` works;
> `aura chat "test" --graph echo` sends the literal text `test --graph echo`
> to the default graph. Same for `aura do`.

That is the day-one path: a single binary, two concepts (skill and channel),
and a local model answering through the full pipeline. For voice, see
[Talking to it](#talking-to-it); to check the node against the frozen
contracts, see [Build & release](#build--release).

---

## Command reference

The `aura` binary is the kernel, the client, the registry, and the fleet tool.

### Running a node

| Command | Purpose |
|---|---|
| `aura up [--port 9080] [--data <dir>] [--mode local\|site\|published] [--memory-budget 8Gi] [--config <file>]` | Start a node. `--memory-budget` enables resource admission; `--config` sets skill defaults (see [Runtime skill config](#runtime-skill-config)). |
| ↳ `[--listen 0.0.0.0] [--no-auth] [--tls-cert <f> --tls-key <f>] [--allow-origin <o>]` | Access control. Default is loopback with a generated bearer token — see [Security model](#security-model). `--listen` and `--no-auth` both widen exposure and both print a warning. |
| ↳ `[--open-witness]` | Let any node anchor its ledger here without a token, rate- and capacity-bounded. See [Open witnessing](#open-witnessing). |
| ↳ `[--anchor default\|<url>] [--anchor-every 1h]` | Keep this node's ledger anchored at a witness in the background, so the guarantee is a property of running the node rather than of remembering a command. |
| ↳ `[--policy <file>] [--max-sessions 1000]` | `--policy` loads an authorization document (deny-by-default for `motor.*`); `--max-sessions` caps live sessions, which matters when an ingress route is public. |
| `aura status [--port 9080]` | Health of a running node plus its connected skills. |
| `aura verify [--data <dir>]` | Recompute the effect ledger's hash chain and Merkle tree, and check every checkpoint signature and witness countersignature — offline, no running kernel required. Exits nonzero if anything fails to verify. See [Security model](#security-model). |
| `aura version` | Version and the protocol/IR majors this binary speaks. |

### Evidence

Everything here is about proving, afterwards, what a node did — see
[Security model](#security-model).

| Command | Purpose |
|---|---|
| `aura witness [witness-url] [--port 9080]` | Anchor this node's ledger with a third party: present the signed head plus a consistency proof, and record the countersignature. With no URL, uses the public witness. Any node can act as a witness. |
| `aura witness audit [witness-url] [--full] [--port 9080]` | Follow a witness and hold it to what it published: verify its signed head, prove its history still extends the one you verified last time, and with `--full` rebuild its tree from the entries it serves. Exits non-zero if it has rewritten anything. See [Who watches the witness](#who-watches-the-witness). |
| `aura witness log [witness-url] [--from 1] [--limit 50]` | What a witness has vouched for, and for whom. |
| `aura receipt <effect-hash> [--out <file>] [--data <dir>]` | Build the portable evidence document for one sealed effect — entry, inclusion proof, signed head, witness countersignatures, cited attestations. |
| `aura receipt --verify <file>` | Check a receipt with no database, no node and no network. What a third party runs. |
| `aura bundle <session> [--out <file>] [--data <dir>]` | Assemble one session's audit bundle: trajectory, a verifiable receipt per sealed effect, and the model configurations behind them. |
| `aura bundle --verify <file>` | Check a bundle with no database, no node and no network. |
| `aura bom [session] [--out <file>] [--data <dir>]` | CycloneDX 1.6 ML-BOM of the models and skills that actually ran, built from the ledger rather than from configuration. |

### Talking to graphs

Flags must precede the positional message — `aura chat --graph echo "test"`, not
`aura chat "test" --graph echo` (the latter sends the flags as part of the text).

| Command | Purpose |
|---|---|
| `aura chat [--graph chat] [--port 9080] ["message"]` | REPL or one-shot conversation with a graph, streaming replies. |
| `aura do [--yes] [--port 9080] "goal"` | Natural-language goal → plan → registered graph → gated execution. |

### Connecting existing software

| Command | Purpose |
|---|---|
| `aura connect --openapi <url\|file> [--name x] [--base-url y] [--header "K: V"]` | Introspect an API and project its operations as skills. |
| `aura observe --target <url> [--port 8080] [--out observed.jsonl]` | Proxy a running system and record the shape of its traffic. |
| `aura generate connector --from <recording> --name <name> [--base-url y]` | Turn a recording into a connector spec, for review. |
| `aura projections [--port 9080]` | List projections and each operation's mode. |
| `aura promote <projection> <op> --mode dry-run\|live\|disabled` | Change one operation's safety mode. |

### The marketplace

| Command | Purpose |
|---|---|
| `aura registry serve [--port 9091] [--data <dir>]` | Host a federable package registry. |
| `aura publish <skill-dir> [--registry <url>]` | Sign (Ed25519, auto-keygen) and upload a skill. |
| `aura add <org/cat/name>[@version] \| --capability <cap> [--registry <url>] [--yes]` | Download, verify hash + signature, show permissions, install. |
| `aura run <org/cat/name> [--sandbox none\|process\|wasm] [--env NAME] [--port 9080]` | Start an installed skill against the local node — spawns a process for `format: source`, or hosts it inside the running kernel's wazero sandbox for `format: wasm` (see [Wasm skills](#wasm-skills)). |

### Debugging & observability

| Command | Purpose |
|---|---|
| `aura why [session] [--no-explain] [--port 9080]` | Walk a session's causal chain to the root cause; narrate it with the local LLM. |
| `aura replay <session> [--graph <id>] [--deny-gates] [--port 9080]` | Re-run recorded real inputs on the current graph and diff outputs, then diff the two sessions' sealed ledger entries against each other — same capability, decision and outcome, not just the same transcript (see [C4](spec/c4-ledger.md)). |
| `aura trace <session> [--otlp <url>] [--out <file>] [--port 9080]` | Export the causal log as OpenTelemetry traces. |
| `aura undo <session\|receipt> [--yes] [--port 9080]` | Reverse one effect, or every reversible effect of a session (reverse causal order), by re-delivering its original payload to the skill's declared `compensates` port. Each undo is itself a gated, sealed effect — see [C4](spec/c4-ledger.md). |

### Fleet

| Command | Purpose |
|---|---|
| `aura federate <remote-url> [--capability <cap>] [--port 9080]` | Proxy a remote node's skills into the local node. |

### Guarding an agent

| Command | Purpose |
|---|---|
| `aura guard --config <mcp-servers.json> [--dry-run] [--trust-annotations] [--port 9080]` | Front the MCP servers an agent already uses, so every tool call passes this node's policy, gate and ledger. `--dry-run` reports what would be registered and changes nothing. See [Guarding an agent's tools](#guarding-an-agents-tools). |
| `aura approvals [--json] [--port 9080]` | List the calls currently waiting for a human. |
| `aura approve <id> [--as <operator>] [--deny] [--port 9080]` | Answer one of them. With `--as`, the answer is signed with that operator's key and sealed into the entry — see [Signed approval](#signed-approval--who-allowed-it). |

### Who may approve

| Command | Purpose |
|---|---|
| `aura operator enroll <id> [--name "…"] [--port 9080]` | Generate a keypair under `~/.aura/operator/<id>` and enrol its **public** half. The node never holds the private key. |
| `aura operator add <id> --pubkey <base64> [--name "…"]` | Enrol a key generated elsewhere — the laptop-approves-a-remote-node case. |
| `aura operator list [--port 9080]` | Who may approve on this node, and who was revoked when. |
| `aura operator revoke <id> [--port 9080]` | Stop them approving anything further. Approvals they already gave stay valid and still verify. |
| `aura operator whoami <id>` | Print this operator's public key from the local keypair. |

### Credentials

| Command | Purpose |
|---|---|
| `aura secret set <name> [value] [--port 9080]` | Store a credential in the kernel, encrypted with the node identity key. Reads stdin when the value is omitted, keeping it out of shell history and the process table. |
| `aura secret ls [--port 9080]` | List configured secret **names**. There is no command, and no route, that returns a value. |
| `aura secret rm <name> [--port 9080]` | Forget one. |

Reference a stored secret as `${secret:<name>}` in a projection header or
connector spec; it is resolved at call time against the receipt of the effect
being delivered. See [The credential broker](#the-credential-broker).

### Regression

| Command | Purpose |
|---|---|
| `aura regress [--sessions 25] [--graph <id>] [--json] [--fail-on-regression] [--deny-gates] [--port 9080]` | Replay recorded sessions against the current graphs and diff the **sealed effects**, grouped by capability and by the model each side cited. See [Regression testing against the ledger](#regression-testing-against-the-ledger). |

---

## Worked examples

End-to-end walkthroughs that chain the commands into something real. Each is
self-contained; run them from the repo root with the kernel built.

### Example 1 — A file-shaped voice loop (WAV → reasoning → WAV)

Compose three skills into one graph so a recorded sentence comes back as
recorded speech. This is the *batch* shape, deliberately: it uses the whole-file
ports (`ears.audio_in`, `mouth.audio_out`), so it is easy to drive with `curl`
and easy to read. The live, streaming version is the seeded `voice` graph — it
adds a fourth skill and uses the chunk ports instead; see
[Talking to it](#talking-to-it).

```powershell
# Start the node, then run the three driver skills (each in its own terminal):
.\kernel\aura.exe up
$env:PYTHONPATH="sdk\python\src"
cd skills\asr;  python main.py    # sensorial.asr.transcribe
cd skills\llm-chat; python main.py    # cognitive.llm.chat
cd skills\tts;  python main.py    # motor.tts.speak
```

Register a graph that wires them, then drive it (WAV in → WAV out):

```json
POST /v1/graphs
{
  "ir": "1", "graph_id": "voice-file", "origin": { "kind": "declared" },
  "nodes": [
    { "ref": "ears",  "resolve": "sensorial.asr.transcribe" },
    { "ref": "brain", "resolve": "cognitive.llm.chat" },
    { "ref": "mouth", "resolve": "motor.tts.speak" }
  ],
  "edges": [
    { "from": "client.audio_out", "to": "ears.audio_in" },
    { "from": "ears.text_out",    "to": "brain.text_in" },
    { "from": "brain.text_out",   "to": "mouth.text_in", "gate": "none" },
    { "from": "mouth.audio_out",  "to": "client.audio_in" }
  ]
}
```

Use a `graph_id` of your own — registering one called `voice` would overwrite
the seeded streaming graph. The `"gate": "none"` is not optional decoration:
speaking is `motor.*`, so without it the kernel gates the edge and the reply
waits for a human click ([Security model](#security-model)).

The two model hops stream internally, but the audio at each end of *this* graph
is a whole file — a reply is spoken once the clause is synthesised, not while
it is being written. The whole exchange is recorded either way: `aura why
sess-…` narrates it, `aura trace` ships it to your observability stack.

### Example 2 — Make a legacy API AI-operable

Take an existing API from zero to "run it by talking to it" in three commands.

```powershell
.\kernel\aura.exe connect --openapi https://crm.internal/api/openapi.json --name crm
#  projection "crm" registered: 40 operations — reads live, writes disabled

.\kernel\aura.exe promote crm create-user --mode dry-run     # inspect first
# then, once you trust it:
.\kernel\aura.exe promote crm create-user --mode live

# with the planner skill running:
.\kernel\aura.exe do "create a user named Grace Hopper in the crm"
#  plan › Use motor.api.crm.create_user …
#  gate › Approve delivery to s1.request_in?  [y/N]: y
#  result ‹ { "ok": true, "status": 201, "body": { "id": 99, "name": "Grace Hopper" } }
```

The CRM's code was never touched; the write happened only after a human approved.

### Example 3 — Build, publish, and install a skill

Author a skill, publish it signed, and install it by capability elsewhere.

```powershell
# scaffold: a directory with skill.yaml + main.py (see "Writing a skill")
.\kernel\aura.exe registry serve                       # host a registry (port 9091)
.\kernel\aura.exe publish .\my-skills\invoice-ocr\     # zip + sign (Ed25519) + upload
#  published acme/vision/invoice-ocr@1.0.0 — signed with key QMJ…

# on another machine / node:
.\kernel\aura.exe add --capability sensorial.ocr --registry http://registry.internal:9091
#  resolved capability "sensorial.ocr" → acme/vision/invoice-ocr
#  permissions: egress_http []  filesystem none  → install? [y/N]: y
.\kernel\aura.exe run acme/vision/invoice-ocr
```

Republishing the same version with different bytes is refused; a different key
cannot hijack the package id.

### Example 4 — Edge + cloud with federation

Keep private inference local, borrow heavy reasoning from a bigger node.

```powershell
# On the edge box (has the OCR skill, keeps documents on-prem):
edge> .\aura.exe up --port 9080

# On the cloud node (has a large LLM):
cloud> .\aura.exe up --port 9080

# From the edge box, borrow the cloud's reasoning capability:
edge> .\aura.exe federate http://cloud-node:9080 --capability cognitive
#  → the edge node now resolves cognitive.llm.chat, executed on the cloud node,
#    while OCR stays local. If the link drops, the edge keeps serving what it has.
```

### Example 5 — Expose everything to Claude Code / Cursor

Every connected skill becomes an MCP tool with one line.

```powershell
claude mcp add --transport http aura http://localhost:9080/mcp
#  In your MCP client, tools/list now shows logical_echo, cognitive_llm_chat,
#  sensorial_api_crm_list_users, …  — read tools run directly; action tools
#  (motor.*) are refused pending human approval, by design.
```

**These five are illustrations of the shape, not copy-paste recipes** — they
assume an API, a camera or a registry you supply. For a path that runs as
written, see [Install & quickstart](#install--quickstart); for skills you can
read and copy today, see [`skills/`](skills/) — [`skills/echo/`](skills/echo/) is
the minimal one, [`skills/postgres-cdc/`](skills/postgres-cdc/) a `sensorial`
skill wrapping an external system, [`skills/model-manager/`](skills/model-manager/)
a `motor` skill that acts on the world.

---

## The control plane UI

The frontend ([`ui/`](ui/), React 19 + Vite + TypeScript, internationalized with
react-i18next — English base, Spanish included) is **built into the binary** via
`go:embed`. `aura up` serves it at `http://localhost:9080` with no Node process at
runtime. Seven views:

- **Chat** — pick a graph, stream a conversation, answer human-approval gates
  inline with Approve/Deny buttons.
- **Voice** — talk to the `voice` graph. Your words appear as you speak them,
  the reply is spoken back as it is written, and speaking over it stops it.
- **Operate** — the `aura do` flow with a UI: type a goal, watch the planner's
  reasoning and steps, then the gated execution and result.
- **Skills** — the live capability catalog, color-coded by type, with each
  skill's ports, schemas, and description.
- **Projections** — connect an OpenAPI spec by pasting it, and flip each
  operation between `disabled` / `dry-run` / `live`.
- **Graphs** — the registered graphs and their IR.
- **Sessions** — every session with error badges; click one to inspect its causal
  event log inline (the raw material behind `aura why`).

To develop the UI against a running kernel:

```powershell
cd ui
npm install
npm run dev      # Vite dev server on :3000, proxying API + WS to the kernel
npm run build    # emits into kernel/internal/gateway/ui/dist (re-embed: rebuild the kernel)
```

---

## Writing a skill (the SDK)

A skill is the smallest publishable unit. It is a directory with a manifest
(`skill.yaml`), the code, and its dependencies. The Python SDK
([`sdk/python/`](sdk/python/), the `aura` package) handles the connection,
registration, causality, and idempotency — you write handlers.

**`skill.yaml`** (contract C1) declares identity, type, capability, and typed
ports:

```yaml
id: "acme/logical/uppercase"       # org/category/name — a global identity
version: "1.0.0"
protocol: "1"

name: "Uppercase"
description: "Uppercases the text it receives."   # the planner READS this
capability: "logical.uppercase"                   # <type>.<function> — resolvable
type: logical

format: source
runtime: { language: python, version: ">=3.11" }

ports:
  ingress:
    - name: text_in
      schema: "std/text@1"                        # every port MUST declare a schema
  egress:
    - name: text_out
      schema: "std/text@1"

permissions:                                      # capability-based; unlisted = denied
  egress_http: []
  filesystem: none
  channels: declared-only
```

**`main.py`** — connect, register, handle:

```python
from aura import Context, Skill

skill = Skill()                    # reads skill.yaml from the working directory

@skill.on("text_in")
async def handle(ctx: Context) -> None:
    text = (ctx.payload or {}).get("text", "")
    await ctx.emit("text_out", {"text": text.upper(), "final": True})

if __name__ == "__main__":
    skill.run()                    # connects to the kernel, reconnects forever
```

Run it against a node with `AURA_WS_URL=ws://localhost:9080/ws/skill python main.py`,
or install and run it from the registry with `aura run acme/logical/uppercase`.

**Key design points.** Skills connect *out* to the kernel (inversion of control),
so they traverse NAT and corporate firewalls without inbound ports. The `Context`
gives you `emit` (a causally-linked reply), `status`, `error`, and `done`; every
message it sends is automatically chained to the one that caused it and given an
idempotency key. Handlers should be idempotent — delivery is at-least-once by
contract.

### Standard schemas

Ports reference versioned schemas. The `std` namespace ships these, as
executable JSON Schemas in [`spec/schemas/std/`](spec/schemas/std/) that the
conformance suite checks — not just as prose:

| Schema | Shape |
|---|---|
| `std/text@1` | `{ "text": string, "final": bool? }` |
| `std/status@1` | `{ "state": "working"\|"done"\|"error", "detail": string? }` |
| `std/document@1` | `{ "mime": string, "bytes_b64": string, "uri": string? }` |
| `std/transcript@1` | `{ "text": string, "final": bool, "replace": bool?, "utterance": string?, "confidence": number? }` |
| `std/audio-chunk@1` | `{ "pcm_b64": string, "sample_rate": int, "channels": int?, "final": bool?, "seq": int?, "ref": string? }` |
| `std/api-request@1` | `{ "params": obj?, "query": obj?, "headers": obj?, "body": any? }` |
| `std/api-response@1` | `{ "ok": bool, "status": int, "body": any?, "dry_run": bool?, "error": string? }` |
| `std/confirmation@1` | `{ "question": string, "options": [string], "held": string, "to_ref": string?, "to_port": string? }` |
| `std/plan@1` | `{ "reasoning": string, "graph": <C2 IR>, "inputs": [...] }` |

**`text` and `transcript` are not interchangeable.** In `std/text@1`, `text` is
a *delta* — a streaming skill emits one per token and the consumer
concatenates. In `std/transcript@1` it *replaces* the previous hypothesis,
because a speech recogniser re-decodes its whole buffer and hypothesis 3 is not
hypothesis 2 plus a suffix. Carrying speech partials as `std/text@1` would make
every consumer that concatenates produce garbage, which is why they are
separate schemas on separate ports.

### Wasm skills

`format: wasm` (Phase 3, [kernel/internal/wasmrt](kernel/internal/wasmrt)) is
the other way to write a skill, for one specific case: a synchronous,
`logical`/`motor` transform where you want `permissions.filesystem` to be a
sandbox the kernel actually enforces, not a line `aura add` merely prints
before install. It compiles to a `.wasm` binary (`GOOS=wasip1 GOARCH=wasm go
build`, or any other WASI-targeting toolchain — TinyGo, Rust, C) and is
hosted **inside the kernel process**, never as a separate one: `aura run`
detects `format: wasm` in the installed manifest and `POST`s the compiled
module to the running kernel (`POST /v1/skills/wasm`) instead of spawning
anything.

The guest contract is deliberately narrow — a WASI **command** module (the
same model as a CGI script: one instantiation per delivery, not a long-lived
process), exactly one ingress and one egress port. The kernel writes the
delivered payload to the guest's stdin and closes it; the guest writes its
reply payload to stdout and exits 0, or writes an error to stderr and exits
non-zero:

```yaml
format: wasm                    # instead of source
ports:
  ingress: [{ name: text_in, schema: "std/text@1" }]   # exactly one of each
  egress:  [{ name: text_out, schema: "std/text@1" }]

permissions:
  filesystem: "read:/data/lookup"     # or write:<path>, or omit for none — a
                                       # WASI directory preopen, enforced not declared
  egress_http: ["api.example.com"]    # exact hostnames only; omit or [] for none
```

Both permissions are enforced, not just shown to a human at install time
(`aura add`). `filesystem` is a WASI directory preopen — with no grant a
guest cannot open a single file; with one, it is confined to exactly that
directory. `egress_http` is a purpose-built host import (`env.http_fetch`,
since WASI preview1 has no sockets at all): the guest owns its own request
and response buffers, and the kernel only completes a fetch when the URL's
hostname is an **exact** match in the list — no prefix or suffix matching.
The permission travels on each delivery's own `context.Context`, so two
skills with different grants running at the same time never see each
other's — see [kernel/internal/wasmrt](kernel/internal/wasmrt).

`sensorial`/`cognitive` skills that need to stream stay `format: source` — a
WASI command module runs once and exits, so there is no guest contract yet
for a skill that emits more than one reply per delivery.

---

## Runtime skill config

Every skill can declare tunable parameters — not just LLM knobs like
`temperature`/`max_tokens`, but anything: a timeout, a confidence
threshold, a storage path. A `config` block in `skill.yaml` (C1, additive)
declares what exists; it does **not** declare the current value, since a
published manifest is immutable (C1 rule 1) — a live value can't live
inside it.

```yaml
config:
  - key: "temperature"
    type: float                 # string | int | float | bool | enum
    default: 0.7
    min: 0.0
    max: 2.0
    description: "Sampling temperature."
    restart_required: false     # true = only applies on the skill's next connect
```

Effective values resolve outside the manifest, in ascending priority:
**declared `default`** → **a `--config` file given to `aura up`** (git-friendly,
re-read at every kernel start) → **a live override** set via the UI or
`PUT /v1/skills/config?id=<id>` (persisted by the kernel, survives restarts).
The Skills tab in the control-plane UI renders a form from the declared
schema for any connected skill that has one — no code to write to get an
editor; and the same effective values are what a `--config` file expresses
as plain YAML for reproducible defaults:

```yaml
# aura.config.yaml
skills:
  "deepaxiom/cognitive/llm-chat":
    temperature: 0.3
    max_tokens: 2048
  "deepaxiom/memory/context-window":
    max_context_tokens: 6000
    trim_strategy: "summarize"
```

```powershell
.\kernel\aura.exe up --config aura.config.yaml
```

A skill reads its starting values off `skill.config` (populated from the
kernel's registration ack) and gets pushed a live update — same dict,
in place — if a value changes while it's connected, unless every changed
key is `restart_required`, in which case it takes effect on the skill's
next reconnect. A skill that never declares `config` is unaffected; this is
opt-in and fully backward compatible with every skill written before it
existed. `skills/llm-chat` (`temperature`, `max_tokens`, `system_prompt`,
`context_window`) and `skills/memory-context` (below) are the worked
examples to copy.

---

## Connecting existing software

You do not *write* an adapter for a legacy system — you *generate* one by
introspection.

```powershell
.\kernel\aura.exe connect --openapi https://my-erp.com/api/spec.json
#  projection "my-erp" registered: 214 operations
#    reads ACTIVE (read-only)  ·  writes DISABLED
#    enable one:  aura promote my-erp create-invoice --mode dry-run
```

Every operation in the spec becomes an individual skill: read operations get the
`sensorial.api.*` capability and go live immediately; write operations get
`motor.api.*` and start **disabled**. Path/query parameters and request bodies are
mapped onto `std/api-request@1`, and each operation's description is written so a
planner can read it and know how to call it.

Four safeties protect the target system, always:

1. **Read-only by default** — nothing writes until you say so.
2. **Explicit per-operation promotion** — you enable one write at a time.
3. **Dry-run mode** — `--mode dry-run` returns the exact request it *would* send
   without sending it.
4. **Human approval** — every edge into a write is gated by the kernel itself
   (see [the motor-gate invariant](#security-model)).

```powershell
.\kernel\aura.exe projections                                # see modes
.\kernel\aura.exe promote my-erp create-invoice --mode dry-run
.\kernel\aura.exe promote my-erp create-invoice --mode live  # real writes now
```

The projection host runs in the binary but talks to the kernel as an ordinary
client — it has no special privileges, and the target system's code is never
touched.

### When there is no OpenAPI document

Introspection needs something to introspect. The common case is the opposite:
one endpoint on a backend nobody documented. Writing a full OpenAPI spec for an
API you only need three calls from is not a reasonable price, and neither is
hand-writing a skill.

A declarative connector — a small skill that reads a few lines of YAML and
registers each operation as an ordinary skill — does the same job as the
OpenAPI projection without an OpenAPI document. It is not part of this
repo's first-party skills, but the shape is a few lines of `Skill`/`Context`
glue (see [Writing a skill](#writing-a-skill-the-sdk)) reading a spec like:

```yaml
name: legacy-erp
base_url: http://erp.internal
headers:
  Authorization: "Bearer ${ERP_TOKEN}"      # from the environment, never the file
operations:
  - { op_id: get-order,    method: GET,  path: /orders/{id}, params: [{name: id, in: path}] }
  - { op_id: create-order, method: POST, path: /orders }
```

and registering `sensorial.api.legacy_erp.get_order` (live) and
`motor.api.legacy_erp.create_order` (disabled until promoted) against it —
same four safeties, same capabilities, same gate as the OpenAPI path. The
difference is where it runs: this is a **skill**, not kernel code, so it
installs from the registry like anything else and never grows the binary
that ships to an edge device.

Promotion needs no new mechanism either — a per-operation `mode` is declared
[runtime config](#runtime-skill-config), so the kernel validates it, persists it
across restarts, and pushes it to the running connector live:

```powershell
# or just flip it in the Skills tab of the UI
curl -X PUT "localhost:9080/v1/skills/config?id=connector/motor/legacy-erp-create-order" `
     -H "Content-Type: application/json" -d '{\"mode\":\"dry-run\"}'
```

Credentials are read from the environment (`${VAR}`), never written in the spec,
and are never served back by the API — `GET /v1/projections` redacts header
values while keeping their names, so an operator can see that auth is configured
without the control-plane handing out the key.

### Not writing the spec either

If the system is already running and already being used, you can skip the YAML
too. Put a proxy in front of it, use the app normally, and the shapes of its
real calls become the connector:

```powershell
.\kernel\aura.exe observe --port 8080 --target http://localhost:3000
#   point your app at :8080 and click around for a few minutes

.\kernel\aura.exe generate connector --from observed.jsonl --name my-app
#   6 call(s) observed → 2 operation(s): 1 read, 1 write
```

Derived-from-traffic beats hand-written for the same reason `aura replay`
exists: recorded traffic is what the system actually does, not what someone
believed it did. Calls that differ only by an id are folded into one operation
with a `{param}`.

**What a recording contains is narrow by construction.** Only method, path,
query parameter *names*, the *shape* of request bodies (key names and JSON
types), and status codes are written down. Never a header value, never a query
value, never a body value — so a recording is usually something you can paste
into an issue without scrubbing it. One caveat that is on you: the path is
recorded verbatim, so an API that puts a token *in the path* puts it in the
recording.

The generated file is opened for review and never executed. Everything in it is
an inference about someone else's system, so a human confirms it before it can
act — the same principle as writes starting disabled.

### When the app is yours

Both paths above wrap a system from the outside. When you own the code, the
cheapest integration is the app announcing itself — no spec to write, no adapter
to keep in sync with the logic it wraps:

```ts
import { createNode } from "@deepaxiom/aura";

const aura = createNode({ org: "acme", app: "shop" });

aura.expose("get-order",    ({ id }) => db.orders.find(id), { params: ["id"] });
aura.expose("create-order", (input) => db.orders.create(input), { write: true });

await aura.start();
```

That is the whole integration. Those two functions are now
`sensorial.api.shop.get_order` and `motor.api.shop.create_order`: resolvable in
graphs, readable by the planner, callable as MCP tools, and — because the second
declares `write: true` — gated for human approval on every edge that reaches it,
including in graphs whose author forgot to ask for one.

The package ([`sdk/node/`](sdk/node/), `@deepaxiom/aura`) has **no runtime
dependencies** — it uses the WebSocket built into Node 20+ and the browser — and
its types are generated from `spec/schemas/`, so they cannot drift from the
frozen contracts. It also carries a typed session client, which is what a
frontend uses instead of copying the control plane's `useSession.ts`:

```ts
const session = await openSession("chat", { onEnvelope: render });
const id = session.sendText("hello");
session.cancel(id);            // abandon it; nothing further arrives
```

A thrown error becomes `{ ok: false, status: 500, error }` rather than a dead
skill: the caller asked a question and gets an answer it can act on.

### Events coming the other way (webhooks)

Skills dial *out* to the kernel, which is what lets them run behind NAT with no
inbound ports. Webhook senders do the opposite: Stripe or GitHub dials in, and
cannot reach a skill that has no address. So inbound HTTP is the one piece of
connectivity that has to live in the kernel — it is transport, not integration.

Declare a route, and the kernel gives you a URL to hand to the sender:

```powershell
$env:STRIPE_SECRET = "whsec_..."          # the kernel reads it, never stores it

curl -X POST localhost:9080/v1/ingress -H "Content-Type: application/json" `
     -d '{\"name\":\"stripe\",\"graph\":\"billing\",\"secret_env\":\"STRIPE_SECRET\"}'
#  → { "url": "/hooks/stripe", ... }
```

Every delivery is HMAC-SHA256 verified against that secret, compared in
constant time, and opens **its own session** on the target graph — so a webhook
gets the same causal event log as anything else and `aura why <session>`
explains what one event caused. A valid signature replayed over a modified body
is rejected, which is the attack that matters.

Verification is on by default: a route with no `secret_env` is refused at
declaration rather than quietly becoming an unauthenticated public endpoint that
injects into a graph. Accepting unsigned deliveries is possible but has to be
asked for (`"unsigned": true`). Routes are listable and revocable
(`DELETE /v1/ingress/{name}`) — an inbound URL you cannot revoke is a problem in
itself — and listing never returns the secret, only the name of the variable it
comes from.

### A row change becomes a causal event (Postgres CDC)

Legacy systems with no API are common enough to name as their own case:
[`skills/postgres-cdc`](skills/postgres-cdc) turns Postgres's own logical
replication stream into ordinary `sensorial.postgres.cdc` events — a skill,
like every connector above, not a kernel feature.

```bash
ALTER SYSTEM SET wal_level = logical;   # once, on the source database; then restart it

PG_CDC_DSN="host=... dbname=... user=... password=..." \
    PYTHONPATH=sdk/python/src python skills/postgres-cdc/main.py
```

It uses `test_decoding` — built into Postgres core since 9.4, so there is
nothing to install on the target database. One `watch_in` message starts the
stream; it keeps emitting `std/db-change@1` events (`{ table, op, columns,
lsn }`, one per row change) until the session cancels it. `config.tables`
narrows it to specific `schema.table`s; the default is every table in the
database. The DSN is read from `PG_CDC_DSN`, not a `config` field — `config`
values are readable over `GET /v1/skills/config`, and a connection string
carries a password.

---

## Talking to it

`aura up` seeds a `voice` graph, and the control-plane UI has a **Voice** view
that drives it. Start the four skills it resolves, press start, and speak:

```powershell
$env:PYTHONPATH = "sdk\python\src"
cd skills\asr;              python main.py    # sensorial.asr.transcribe
cd skills\llm-chat;         python main.py    # cognitive.llm.chat
cd skills\sentence-chunker; python main.py    # logical.text.sentence_chunk
cd skills\tts;              python main.py    # motor.tts.speak
```

Your words appear as you say them, the reply is spoken as it is written, and
speaking over it stops it. Nothing about that needs a cloud account.

**Five client ports on one socket, at once** — audio up, audio down, partial
transcripts, reply text, status (the last one fed by two edges, from the
recogniser and from the synthesiser). That is what "multi-channel" means here,
and the kernel has always supported it; the voice graph is the first thing to
use it. Each edge declares what it needs:

```jsonc
{ "from": "client.audio_out",      "to": "ears.audio_chunk_in" },              // reliable: losing input audio degrades recognition invisibly
{ "from": "clause.text_out",       "to": "mouth.text_in",     "gate": "none" }, // speaking is motor.*; say out loud that it needs no click
{ "from": "mouth.audio_chunk_out", "to": "client.audio_in",   "qos": "realtime" } // a stale frame is worthless
```

**Interruption is the part that is hard, and it is why the rest exists.** When
you speak over the assistant, the browser stops playback locally first —
instantly, with a 20ms fade so the cut does not click loudly enough to
retrigger the microphone — and only then tells the kernel to abandon the chain.
The kernel stops routing everything belonging to it at any depth, so a skill
that ignores the cancel and keeps generating produces output that goes nowhere.
Measured: a new question preempts the previous answer in ~0.2s.

Some things worth knowing before you plug in a microphone:

- **A microphone needs a secure context.** `localhost` counts; a LAN address
  over plain `http://` does not, and fails with a confusing permissions error.
- **Use headphones, or tick "mute while speaking".** Browser echo cancellation
  is good but not perfect, and an assistant that hears itself will interrupt
  itself.
- **Set the ASR language** if you know it. Autodetect on a short utterance is
  measurably worse — the same sentence transcribes cleanly with a hint and as
  near-gibberish without one.
- **piper is effectively required for voice that feels live.** The OS voice
  works and is chunked the same way, but it can only synthesise whole files, so
  sound starts later.

---

## Natural-language operation

With the planner skill running (`skills/planner/`), `aura do` closes the loop from
a spoken goal to a gated, executed result:

```
> aura do "create a user named Grace Hopper in the crm"
  plan › Use motor.api.crm.create_user to create the user.
  step › s1 → motor.api.crm.create_user
  gate › Approve delivery to s1.request_in?  [y/N]: y
  result ‹ { "ok": true, "status": 201, "body": { "id": 99, "name": "Grace Hopper" } }
  goal completed
```

The division of labor is deliberate and is what makes it reliable even with a
small local model: **the LLM only chooses and extracts** (which skills, which
parameters — the fuzzy part), and **code compiles the plan** into a valid graph
(the exact part). Safety policy lives in the code, not the prompt: every edge into
a `motor.*` skill gets a `human-approval` gate automatically. The planner also
repairs minor hallucinations (a wrong capability prefix with a single real match)
and normalizes misplaced parameters against each projection's declared shape.

Planner backends form a declared degradation chain: `openai` → `local` (any GGUF
via llama.cpp) → `keyword` (a deterministic, no-model fallback). The last one
means planning works even with no model installed at all.

---

## Typed ports, enforced

C1 makes a schema mandatory on every port, and C2 rule 2 refuses to wire two
ports whose schemas disagree. So the runtime knows, *statically*, the exact
shape of everything a skill may emit. Two things follow that no other agent
runtime is in a position to offer, because none of them have mandatory typed
ports.

**A decoding grammar derived from the port's type.** The kernel compiles each
port's JSON Schema into a GBNF grammar and hands it to the skill at
registration. A skill that generates under it *cannot* emit a shape the port
would reject — not "rarely does", cannot:

```python
grammar = skill.grammar_for("plan_out")          # pushed by the kernel at registration
out = llm.create_completion(prompt, grammar=grammar)
```

```powershell
curl localhost:9080/v1/grammars/std/status@1
#  root-state-1 ::= "\"working\"" | "\"done\"" | "\"error\""
#  root ::= "{" ws "\"state\"" ws ":" ws root-state-1 ws ( ... )? "}"
```

Constrained decoding itself is not new — XGrammar, llguidance and Outlines all
do it well, and every serving stack ships one. What is unusual is *where the
grammar comes from*. Everywhere else it comes from a schema the application
author wrote by hand and passed to the inference call, so it is only as correct
as their discipline and it drifts from whatever consumes the output. Here it is
derived from the type of the port the output is going into — the thing that
will actually reject it.

Property order follows the schema's own declaration order rather than
alphabetical, deliberately: `std/transcript@1` declares `text` before `final`,
and a model made to commit to `final` before writing the text it describes
generates worse — especially the small local models this runtime targets.

**Validation for everything that does not generate.** The same schema checks
payloads on the way past, so a skill building a dict by hand, a projected API
returning a surprise, or a client posting anything at all is held to the port's
type too. The grammar makes a violation unreachable; the validator makes it
rejected. Both halves are needed for "typed channel" to be a claim rather than
a description.

The compiler covers the subset the `std` namespace uses and **refuses anything
outside it** rather than emitting a permissive grammar — a grammar that allows
more than the schema does is worse than none, because it looks like a
guarantee.

---

## Scheduling: speculation, deadlines, budgets

### Speculative graph execution

The dominant cost in an agent graph is sequential waiting: a model streams for
two seconds and only then does the next skill begin. Starting that skill early
on the prefix is an obvious win and a well-studied one — PASTE, SPORK, SpecBox
and a whole 2026 literature. Every one of those systems spends most of its
effort on the same question: **which steps are safe to run before we are
sure?** Running a tool early that sends an email or charges a card is not a
latency optimisation, it is a bug with a stopwatch. They answer with
heuristics, allow-lists, or an LLM's judgement.

This runtime does not have to ask. C1's five skill types are an **effect type
system** — `motor` is precisely "acts on the world" — so the answer is already
in the manifest, statically, for every skill in the graph:

```json
{ "from": "brain.text_out", "to": "summariser.text_in", "speculative": true }
```

```
edge brain.text_out -> writer.text_in declares speculative but
"acme/motor/erp-writer" is a motor skill; work that acts on the world is never
run ahead of certainty, because a discarded effect is not discarded
```

That refusal happens at wiring, before a session exists. It is the motor-gate
invariant seen from another angle: the type of skill that must wait for a human
is the type that must never run ahead of certainty.

Reconciliation reuses machinery that already exists. The kernel folds partials
into a running value using the schema's own semantics (appending for
`std/text@1`, replacing for `std/transcript@1` — the distinction C1 already
draws), delivers it marked complete, and when the producer finishes either
suppresses the redundant delivery or abandons the speculative chain through the
ordinary `cancel` path. A skill that handles cancel correctly handles a
speculation miss correctly for free, and the kernel's suppression guarantee
means a skill that ignores it cannot leak a discarded result into a real one.

Hit and miss counts are reported, because a feature that spends compute on
guesses should have to show its record. A node can refuse speculation wholesale
with `speculation: deny` in its policy.

### Deadlines that propagate

```json
{ "from": "client.text_out", "to": "brain.text_in", "deadline_ms": 800 }
```

Converted to an absolute instant at the hop that declares it and inherited
downstream, so a three-hop chain cannot quietly grant itself three times the
budget by adding hops. A hop may tighten it, never extend it. The receiving
skill is expected to **degrade** — fewer beams, a smaller model, a coarser
quantization — rather than abort: a worse answer in time beats a better one
after nobody is listening.

Deadline propagation has been standard in RPC for a decade and is absent from
every agent runtime, which is why a slow tool there degrades into a hang
instead of into a cheaper answer.

### Which model answered, decided by policy

```yaml
routes:
  - match: "cognitive.llm.*"
    prefer: ["deepaxiom/cognitive/llm-chat-small"]
    reason: "the small model handles the default graph; escalate by graph, not by luck"
```

Model choice is a governance question, not only an engineering one — "which
model answered this" is a thing an auditor asks — so it belongs in the same
signed document that says what may act on the world, readable by inspection.

This is deliberately **not** a learned router. RouteLLM-style classifiers and
the vLLM semantic router pick per query and are genuinely better at cost and
quality; they are also unauditable by inspection, which is the property traded
away here. The two compose: put a learned router behind a `cognitive.*`
capability and route to it.

### A context budget the kernel enforces

```json
{ "ir": "1", "graph_id": "long-session", "context_budget": 8000, "nodes": [] }
```

A budget enforced by the skill is a budget that holds only for skills that
remembered to implement it, and the failure mode differs everywhere — a
truncation in one, a 413 in another, silent data loss in a third. One place,
one rule, one error message.

Token counting is explicitly an **estimate** (bytes/4, the usual BPE rule of
thumb). The kernel has no tokenizer and should not grow one, since that would
couple it to a model family. A budget presented as exact would be trusted for
capacity planning it cannot support.

---

## Model drivers & resource admission

Voice arrives as first-party skills — each a thin driver over a native
runtime, each with a declared degradation chain:

| Skill | Capability | Engine |
|---|---|---|
| [`skills/llm-chat`](skills/llm-chat/) | `cognitive.llm.chat` | llama.cpp (any GGUF) by default; any OpenAI-compatible API (OpenAI, Gemini, …) if `OPENAI_API_KEY` is set. Per-session history. |
| [`skills/asr`](skills/asr/) | `sensorial.asr.transcribe` | faster-whisper (CPU int8). Takes a whole WAV on `audio_in`, or a live PCM stream on `audio_chunk_in` with partial transcripts while the person is still speaking. Ends an utterance on the client's signal, on trailing silence, or on a hard cap. |
| [`skills/tts`](skills/tts/) | `motor.tts.speak` | piper (opt-in) → OS voices (SAPI/espeak). Emits PCM in ~200ms chunks for a live listener whatever the backend is, plus the whole clause as a WAV. |
| [`skills/sentence-chunker`](skills/sentence-chunker/) | `logical.text.sentence_chunk` | Groups a token stream into clauses. Put it between a streaming LLM and anything that works in utterances, or the synthesiser fires once per token. |
| [`skills/model-manager`](skills/model-manager/) | `motor.models.manage` | list / catalog / HF search / download / delete |
| [`skills/memory-context`](skills/memory-context/) | `memory.context.window` | SQLite (embedded, local file) — persists per-session turns across restarts, `recall` hands back a window trimmed to a configurable token budget (drop-oldest, or summarize via `aura.llm.ChatBackend`). llm-chat's own history is in-process and unbounded (see its `main.py`); this is the durable, budget-aware alternative — wire it into a graph explicitly, it is not auto-connected. |
| [`skills/postgres-cdc`](skills/postgres-cdc/) | `sensorial.postgres.cdc` | Turns Postgres's own logical replication stream (`test_decoding`, nothing to install) into `std/db-change@1` events, one per row change. |

Because they all share the port/schema interface, a graph asking for
`sensorial.asr.transcribe` never cares which engine answers — that is the model
plane in practice. If you want something that runs with nothing at all to wrap
a new external system, start from [`skills/echo/`](skills/echo/) — no model,
no credentials, no network.

**Resource admission (finite hardware).** A node started with a memory budget
refuses to admit skills that would not fit, with an explanation instead of a
silent out-of-memory kill:

```powershell
.\kernel\aura.exe up --memory-budget 1536Mi
#   a skill declaring requirements.memory: 1Gi is admitted;
#   a second 1Gi skill is refused:
#   "admission rejected: … declares 1.0Gi but only 512Mi of the 1.5Gi budget remains …"
#   the reservation is released when the skill disconnects; usage shows in /healthz
```

---

## Audit bundles

A 2026 study of agent benchmark protocols ([arXiv 2607.22368](https://arxiv.org/abs/2607.22368))
examined published traces and found **67% contained "protocol exposures"** —
paths by which a score could be earned without the capability being measured.
Its conclusion was not that benchmarks need better tasks but that reports need
to ship the evidence required to interpret them. It names what a runtime has to
emit for that to be possible:

| The study asks for | This runtime already had it, for other reasons |
|---|---|
| Complete trajectory logs — calls, ordering, timestamps | The causal event log (C3 rule 7) |
| Artifact provenance, with hashes | The effect ledger (C4) |
| Replayable model configuration | The inference attestation (C5) |
| Comparison baselines from paired runs | `aura replay` |

What was missing was one document carrying all four that verifies on its own.

```powershell
.\kernel\aura.exe bundle sess-8f3a --out case.json
#  session    sess-8f3a
#  steps      412
#  effects    3 sealed · 2 delivered · 1 denied
#  models     [Qwen/Qwen2.5-1.5B-Instruct-GGUF (Q4_K_M) @f1d2d2f924e9]

.\kernel\aura.exe bundle --verify case.json     # anyone, anywhere
```

A **receipt** proves one effect. A **bundle** explains one session: the
trajectory, a standalone receipt per sealed effect, and the model
configurations that argued for them. Editing, dropping or reordering a single
step fails verification, which is precisely the edit someone would make to hide
how a score was earned.

Truncation is declared, never hidden. A session longer than the step cap
produces a bundle that says it is a prefix — everything present still verifies,
and the reader is told what they are not looking at.

---

## An OpenEnv environment

[OpenEnv](https://github.com/huggingface/OpenEnv) is Hugging Face's contract
for agentic-RL environments: Gymnasium-shaped `reset` / `step` / `state` over
HTTP, consumed by TRL, torchforge and SkyRL. Every registered graph on a node
is one:

```powershell
curl localhost:9080/openenv/spec
#  { "openenv": "1", "environments": [ {"id": "chat"}, {"id": "voice"} ], ... }

curl -X POST localhost:9080/openenv/reset -d '{"environment":"chat"}'
curl -X POST localhost:9080/openenv/step  -d '{"episode_id":"ep-…","action":{"text":"hello"}}'
```

This is a **border**, like the MCP server and the A2A card: it speaks someone
else's protocol at the edge with no privileges inside the kernel.

Two decisions worth stating, because both are refusals:

**Reward is always `null`.** A runtime cannot know what a task counts as
success, and a fabricated number is exactly the "score with no protocol behind
it" the study above is about. The field is present so a wrapper can fill it
from the observation.

**The episode's audit bundle is available at
`/openenv/bundle?episode=<id>`.** That is the reason this border is worth
having rather than merely conforming: an environment that returns
tamper-evident evidence alongside the observation lets a benchmark report the
assumptions behind a score, not only the score.

---

## Signed approval — who allowed it

The security model has always been able to prove *that* an effect was gated and
answered. It could not prove **who answered**, and that gap was larger than it
looks: a compliance review does not ask whether a human approved, it asks which
one. "A human approved" without a name is a log, not an audit trail.

It was also unprovable in principle rather than merely missing. The node writes
its own ledger, so a node that wanted to claim an approval had happened could
write one. Every other guarantee here rests on a signature the node cannot forge
*on someone else's behalf* — and approval, the one field describing a human
decision, had none.

```powershell
.\kernel\aura.exe operator enroll grace --name "Grace Hopper"
#  enrolled grace
#  public key   MCowBQYDK2VwAyEA…
#  private key  C:\Users\you\.aura\operator\grace\ed25519.key

.\kernel\aura.exe approvals                    # what is waiting
.\kernel\aura.exe approve <id> --as grace      # sign it
#  approved 01J9ZK… — signed as grace
```

The sealed entry now carries the answer:

```json
"approver": {
  "operator": "grace",
  "pubkey":   "MCowBQYDK2VwAyEA…",
  "envelope": "01J9ZK2M1P…",
  "decision": "approve",
  "ts":       1754083200000,
  "sig":      "base64…"
}
```

**The node never holds the private key.** It lives under the operator's own home
directory and only the public half is enrolled. That is what makes an approval
something the node cannot manufacture about itself, which is the entire point —
the party under audit must not be able to produce its own evidence. On a
single-machine install the same person owns both directories, so the separation
is a convention there rather than a boundary; it becomes real the moment the
operator approves from their laptop against a node somewhere else.

Everything in the signed statement is bound, so a valid signature cannot be
moved: not to another node, session, delivery, decision, or time. Two checks
run, and they are deliberately kept apart:

| | What it asks | Against what | When |
|---|---|---|---|
| **Authorship** | did this key sign *this* resolution? | the sealed bytes | forever, offline |
| **Enrollment** | may that operator approve here, under that key? | the live roster | once, at the gate |

Merging them would make history depend on the present. **Revoking an operator
does not invalidate approvals they already gave** — an audit trail that changes
when the org chart does is not one. `aura operator revoke` says so explicitly.

Three refusals worth knowing about, each of which is a real attack:

- A key the node never enrolled, signing as `grace`, produces a
  cryptographically valid statement. It is refused — the name is a claim until
  the roster confirms the key behind it.
- A signed `deny` forwarded with `"approve": true` beside it is refused, rather
  than either field winning. The signature covers the decision precisely so
  that flipping an unauthenticated boolean cannot change the answer.
- An approval for one held delivery, replayed against another in the same
  session, is refused. Within one conversation that is the difference between
  approving one payment and approving the next.

To make signatures mandatory:

```yaml
# aura.policy.yaml
policy: 1
default_effect: gate
require_signed_approval: true
```

Then an unsigned answer to a gate is a **denial**, not a fallback — an
enforcement you can skip by omitting a field enforces nothing. A node with this
set and nobody enrolled **refuses to start**, rather than coming up healthy and
denying its first write minutes later with the cause several layers away from
the symptom.

`aura verify` checks approver signatures in the same offline walk it uses for
the chain. An approval that no longer verifies makes the ledger **unsound**: it
is direct evidence that an entry was edited after sealing, or that one was
written claiming a human said yes when none did.

---

## The credential broker

`aura guard` and node policy share a limitation this guide has always stated
plainly: they hold exactly as far as your control over the agent's config does.
Nothing stops a process from skipping the gate — it just calls the ERP directly.
That makes the checkpoint an excellent safety rail and a weak security boundary,
and no amount of policy fixes it, because policy is enforced at the place the
caller chose to visit.

The usual answer is enforcement at the network: a proxy, eBPF, a sidecar. They
work, they need infrastructure this runtime promises you will not need, and none
of them run on a Raspberry Pi.

The other answer is to stop trying to make the bypass impossible and make it
**useless**. An agent that skips the gate reaches the ERP and has no credential
for it, because the credential was never in its environment — it is in the
kernel, and the only way to get it is to present the receipt of an effect that
just passed the checkpoint.

```powershell
.\kernel\aura.exe secret set erp_token          # reads stdin: no shell history, no process table
#  stored erp_token — reference it as ${secret:erp_token}

.\kernel\aura.exe secret ls
#  ${secret:erp_token}
```

Reference it where the credential used to sit:

```yaml
name: legacy-erp
base_url: http://erp.internal
headers:
  Authorization: "Bearer ${secret:erp_token}"    # resolved at call time, never stored expanded
```

What a receipt proves, without the broker trusting the caller at all:

- **the effect existed and was authorized** — the entry is in the chain, and an
  entry only gets there by passing the checkpoint;
- **it was allowed or approved** — a denied effect buys nothing;
- **it is this effect** — the entry names the capability, so one skill cannot
  spend another skill's approval on the payments token;
- **it is recent** — 90 seconds, redeemable three times. A receipt is not a
  bearer token with a useful shelf life; the allowance exists for a retry after
  a connection error, not for fan-out.

A forged receipt is not in the ledger. A stolen one is bound to someone else's
capability. A replayed one is expired. **A leaked node token does not become a
leaked ERP credential** — holding it lets you set a secret and see that one
exists, and there is no route anywhere that returns a value.

Secrets are encrypted at rest with a key derived from the node's own identity
key, and the name is authenticated alongside the value, so a ciphertext cannot
be moved from one secret's row to another's with a text editor. A data directory
copied without its `identity/` subdirectory yields ciphertext nobody can open,
which is the correct outcome for a stolen database file.

A skill running as its own process uses the same door:

```powershell
curl -X POST localhost:9080/v1/secrets/resolve `
  -H "Authorization: Bearer $token" `
  -d '{"receipt":"sha256:…","capability":"motor.api.erp.create_invoice","names":["erp_token"]}'
```

**What this does not do**, stated plainly: it does not stop a skill that
legitimately received a credential from keeping it. Once a value reaches a
process, that process has it, and nothing short of never revealing it — a
signing oracle, an egress proxy — changes that. What it removes is the
*standing, ambient* credential: the token sitting in a long-running agent's
environment, available for every call it ever makes, gated or not. The window
narrows to one authorized effect.

---

## Regression testing against the ledger

`aura replay` answers "did this one session reproduce what it recorded". That is
the right unit for debugging and the wrong unit for the question teams actually
face every week:

> we are about to change the model. What does that do to the effects?

Nothing on the market answers it with evidence. Eval suites score outputs
against a rubric — which measures whether the text got better and says nothing
about whether the runtime went on to charge a different card. Observability
records the aftermath. Neither can say *"339 of 345 sessions sealed exactly the
same effects; here are the 6 that did not, and here is the model revision on
each side."*

```powershell
.\kernel\aura.exe regress --sessions 200
#    [1/200] sess-8f3a · reproduced (2 effect(s))
#    [2/200] sess-9c1b · CHANGED (1 divergence(s))
#    …
#
#    ── regression report ──────────────────────────────────
#
#    sessions      200 replayed · 194 reproduced · 6 changed · 0 could not run
#    effects       412 sealed before · 406 after   ← 6 fewer effects happened
#
#    models cited
#      before  Qwen/Qwen2.5-1.5B-Instruct-GGUF @f1d2d2f (Q4_K_M)
#      after   Qwen/Qwen2.5-3B-Instruct-GGUF @a7c31e0 (Q4_K_M)
#
#    what changed, by capability
#      motor.payments.refund                    6 session(s)  [count×6]
#
#    sessions that changed
#      sess-9c1b → replay sess-4d2e
#          effect count 3 → 2
#          aura why sess-4d2e
#
#    verdict: 6/200 sessions changed behaviour
```

The line that matters is **"6 fewer effects happened"**, and it is the finding an
output diff structurally *cannot* make: an act that stopped happening leaves no
text behind to compare against, so it reads as silence. Only something counting
the sealed effects can see it.

This works here and nowhere else for a reason that is not cleverness — the
aggregation is a fold over the existing per-session diff. It is that C5 already
binds the model revision, quantization and sampling parameters to every act, so
the comparison is between two *named, pinned* configurations rather than between
"before" and "after", and a divergence arrives with the two attestations that
produced it.

Deliberately not a pass/fail gate by default: a model-backed graph legitimately
words things differently run to run, and a tool that cries regression at every
rephrasing gets muted within a week — which is how it stops being read at the
one moment it matters. `--fail-on-regression` exits non-zero for a CI job that
has decided its graphs are deterministic enough to hold to it, and `--json`
emits the whole report.

Two honest limits:

- A session that **could not be replayed** (its graph is gone, a skill it needs
  is offline) is counted separately and does **not** make the run dirty. Silently
  converting "could not test" into "test failed" trains people to ignore results.
- **Gates are auto-answered unsigned during replay**, so a node with
  `require_signed_approval` will see them refused. That is correct rather than
  unfortunate: a replay that could mint a signed approval would mean the
  machinery for manufacturing one exists, and the value of a sealed approval is
  precisely that it cannot be produced without a human's key.

---

## Skill isolation

This has been the largest open security gap since the first release, and the
README has said so. This narrows it; it does not close it, and the difference
is worth stating precisely.

```powershell
.\kernel\aura.exe run acme/vision/invoice-ocr --sandbox process --env PG_CDC_DSN
#  sandbox: process — scrubbed environment, cwd jail, no inherited handles.
#    Best effort: this stops accidental credential inheritance and casual
#    filesystem access.
#    It does NOT contain hostile code — use `format: wasm` for that.
```

| Backend | Isolation | Portable | Status |
|---|---|---|---|
| `none` | None — the pre-existing behaviour, now explicit | yes | shipped |
| `process` | Scrubbed env, cwd jail, no inherited handles | yes | shipped |
| `wasm` | WASI — `permissions` genuinely enforced by wazero | yes | shipped |
| `microvm` | A real boundary | Linux+KVM | **interface only, refused at startup** |

**What `process` buys.** The environment becomes an allowlist rather than an
inheritance: a skill installed from a registry no longer receives an operator's
`AWS_SECRET_ACCESS_KEY`, `GITHUB_TOKEN` or `SSH_AUTH_SOCK` just by being
started. A skill that genuinely needs a credential names it with `--env`. That
is a real and common failure closed. It is **not** a boundary against hostile
code, which can still reach the network and the filesystem through syscalls no
portable Go runtime intercepts.

**Why `microvm` is refused rather than stubbed.** The 2026 consensus is
unambiguous — containers share a kernel and are not an isolation boundary;
microVMs (Firecracker, Cloud Hypervisor, Kata) or gVisor are what production
uses. That consensus is right. Implementing it needs Linux with KVM, and a
backend that silently degraded to something weaker would be worse than none,
because an operator would run third-party code believing in isolation the
runtime manufactured for them. Asking for it produces an error explaining what
to use instead.

For code you have reason to distrust today, the honest options are
`format: wasm` — where `permissions` really is enforced — or running the node
itself inside a boundary you already trust.

---

## Open witnessing

`aura witness` anchors a ledger with a third party. Until now the endpoint sat
behind the node's bearer token, which meant witnessing only worked between
parties who had already exchanged a credential — a witness inside the same
trust domain as the log it vouches for, which is most of what Certificate
Transparency's model relies on not being true.

```powershell
.\kernel\aura.exe up --open-witness
#  WARN open witness enabled — any node may anchor its ledger here without a token
#       max_nodes=10000 per_node_per_hour=12 retention_days=90
```

What made this safe to offer was not the route change but the bounds. An open
witness is a write surface, and anyone can mint a keypair and invent a node id:

- **Rate**: 12 presentations per node per hour. A node checkpointing normally
  presents far below that.
- **Capacity**: 10,000 distinct nodes. At the cap, nodes already vouched for
  keep working and new ones are refused — the useful failure mode, since the
  value of witnessing is continuity with what you already promised.
- **Retention**: 90 days. Forgetting is also honest: a countersignature whose
  baseline the witness discarded is one it can no longer contradict.

The statement is self-authenticating — it carries the presenting node's public
key and a signature over the head — so an open witness verifies before it
remembers anything, and refuses before it does any cryptography for a caller
over its rate limit.

### Anchoring by default

An anchor protects nothing until somebody creates one, and a guarantee that
depends on remembering to run a command is stale on the day it matters. So a
node can keep itself anchored:

```powershell
.\kernel\aura.exe up --anchor default --anchor-every 1h
#  auto-anchoring enabled  witness=https://witness.deepaxiom.org every=1h0m0s
```

`--anchor default` uses the project's public witness — free, anonymous, no
account. `--anchor https://…` names another. `aura witness` with no argument
does the same thing once, by hand.

Nothing about the public one is privileged. It holds no key of yours, sees no
payload, and its own history is public and checkable with the command below.
Two nodes anchoring each other prove something to each other and nothing to
anyone else; a witness many independent nodes present to is a reference point a
third party can already be following — which is the only reason to prefer it.

---

## Who watches the witness

Anchoring answers "can this node rewrite its own history?" It leaves the
question a sceptical reader asks next, and until this release the honest answer
was *nobody*: a witness kept one record per node, replaced as that node
advanced. A replaced record says nothing about what it used to say, so a witness
could quietly revise what it had vouched for and no one could show it.

Certificate Transparency's answer is not "trust the log operator" — it is that
logs publish their own Merkle history and independent monitors follow them, so a
log that rewrites or forks is *caught*, publicly, by anyone who kept the
previous head. That is what a witness does now.

```powershell
.\kernel\aura.exe witness audit                    # audit the public witness
.\kernel\aura.exe witness audit http://peer:9080   # or any other
#    ok    head signed by a445e1661f20
#    ok    grew 1 → 2 entries, and the old history is still a prefix
#    ok    the published root matches the entries it serves (2 rebuilt)

.\kernel\aura.exe witness log                      # what it has vouched for, for whom
#    SEQ    NODE                     ENTRIES  ROOT
#    1      node-dc25cb65f36bf7d6    2        sha256:007b27d81f3cf90…
#    2      node-dc25cb65f36bf7d6    4        sha256:89d0d75fecfbe1f…
```

Three checks, each a distinct way a witness can be dishonest:

| | Catches |
|---|---|
| The head verifies against the key it claims | following a number somebody typed |
| The new head extends the one you already verified (consistency proof) | an entry dropped or rewritten after publication |
| `--full` rebuilds the tree from the served entries | a witness signing a root unrelated to the log it hands out |

**What the monitor keeps between runs is the point.** A snapshot proves nothing;
a *remembered* snapshot proves a witness has not gone back on it. The baseline
lives on the follower, never on the witness, and only ever moves forward — a
monitor that could be talked into lowering it could be talked into forgetting
the evidence that would convict.

Exercised in CI the way it would actually be attacked, not by calling an API
wrongly: `scripts/witness_adversarial.py` stands up a witness and a node, anchors
real sealed effects, then **rewrites an already-published entry directly in
SQLite** and requires the monitor to refuse and exit non-zero.

```
FAIL  same size (2), DIFFERENT root.
      was  sha256:0ea42ce0769c2dcc…
      now  sha256:1070b0e4c531639b…
      This witness rewrote history it had already published.
```

### The signed answer

The change underneath all of this is small and is the one that matters. A node
asks a witness *how far have you vouched for me?* before building its proof.
That answer used to be a bare integer, which meant a witness could tell the node
one thing and an auditor another and neither could prove it — two rumours.

It is signed now, and the signature covers the witness's own log position at the
moment it answered. **The same two answers become two statements over one key
that cannot both be true.** A split view stops being undetectable and becomes
self-incriminating, which is what lets several parties who do not trust each
other rely on one witness without any of them having to believe it.

"Never seen this node" is signed too. A witness has to be holdable to a denial,
or denial is the one free lie.

What travels on these routes is heads, roots, keys and signatures — no payload,
no capability, no session, no operator. A witness has nothing to leak, which is
what makes one usable by the organisations that most need it.

---

## The marketplace

Skills are distributed through a **federable** registry — anyone can host one with
`aura registry serve`, exactly like a container registry, which is what makes the
neutrality of the ecosystem credible.

```powershell
.\kernel\aura.exe registry serve                 # host a registry (its own port)

.\kernel\aura.exe publish my-skill\              # zip + sign (Ed25519) + upload
.\kernel\aura.exe add acme/vision/invoice-ocr    # verify + review permissions + install
.\kernel\aura.exe add --capability sensorial.ocr # discover by capability, not name
.\kernel\aura.exe run acme/vision/invoice-ocr    # start it against the local node
```

Installing shows you exactly what you are trusting *before* it lands — the
publisher key, and the skill's declared permissions ("can call `api.acme.com` and
nothing else") — then asks for confirmation.

Registry guarantees, all enforced and tested:

- **Immutable versions** — republishing a version with different content is
  rejected; you must bump the version. Republishing identical content is idempotent.
- **Trust-on-first-use** — the first publish binds a package id to its publisher
  key; a later version signed by a *different* key cannot hijack the id, even if
  that signature is otherwise valid.
- **Tamper-proof coupling** — the signature covers the manifest hash *and* the
  artifact hash together, so neither can be swapped independently.
- **Safe extraction** — a path-escaping entry in a package is refused on install.

---

## Explainability & replay

Every session persists an append-only causal event log — each message names the
message that caused it. Two tools read it.

**`aura why`** walks the chain backwards from the failure (or the last activity)
to the root and narrates it with the local LLM:

```
> aura why sess-90615a146927
  FAILED — causal chain from root to failure:
  │ 1. [data]             client.text_out    {"text":"send the confidential report"}
  │ 2. [data]             eco.text_in        {"text":"send the confidential report"}
  │ 4. [confirm_request]  client.confirm_in  {"question":"Approve delivery…"}
  │ 5. [confirm_response] client             {"approve":false}
  6. [error]            client.text_in     {"detail":"delivery … denied by user"}

  why › The root cause is that the user denied the delivery of the confidential report.
```

With no LLM skill connected it degrades cleanly to the annotated deterministic
chain. Called with no session id, it explains the most recent one.

**`aura replay`** turns recorded real traffic into an eval suite — it re-sends a
session's original client inputs into a *new* session of the current graph and
diffs the outputs:

```powershell
.\kernel\aura.exe replay sess-c0884792d2b4          # verdict: 3/3 outputs identical
.\kernel\aura.exe replay sess-… --graph chat-v2     # A/B against a different graph
.\kernel\aura.exe replay sess-… --deny-gates        # exercise the denial path
```

---

## Federating nodes

Each node runs standalone (leaf-node autonomy). Federation *widens* what a node
can resolve by proxying another node's skills as if they were local.

```powershell
# On an edge box (node B): run, say, the OCR skill.
# On your laptop (node A):
.\kernel\aura.exe federate http://edge-box:9080
#   federating http://edge-box:9080 → 1 skill(s) proxied into the local node (route: lan)
#   node A now resolves sensorial.ocr.image — the work runs physically on B,
#   replies stream back with causality intact.
.\kernel\aura.exe federate http://edge-box:9080 --capability sensorial   # filter
```

The bridge is pure userland: it is simultaneously a skill-client of the local node
(registering proxy skills) and a stream-client of the remote node (driving graphs
there), relaying envelopes between them. It required no kernel changes, which is
some evidence that the micro-kernel boundary is drawn in a useful place. It also
is covered by tests (`fed`, 87%), including that `cancel` and `idem` survive
the node boundary.

**Negotiated transport (C3 rule 5).** The bridge keeps one persistent connection
per federated capability, reused across every relay — not redialed per
envelope, which is what it used to do (confirmed by the wire, not assumed: a
five-message exchange meant five WebSocket handshakes and five disconnected
remote sessions). Cancelling one relay sends a targeted `cancel` envelope on
that shared connection rather than closing it — closing it would abort every
other relay riding along — and `aura federate` prints what route it measured
to the remote (`same-host` / `lan` / `relay`, timed against `/healthz`).
Scoped to what's actually verifiable without two genuinely separate networks:
QUIC/WebRTC and NAT traversal remain future work.

---

## Standards at the borders

Deep Axiom speaks the ecosystem's protocols on its edges, so it interoperates
instead of competing with them.

**MCP server.** Every connected skill is automatically a tool. Plug Deep Axiom
into Claude Code, Cursor, or any MCP client:

```powershell
claude mcp add --transport http aura http://localhost:9080/mcp
#   tools/list → logical_echo, cognitive_llm_chat, sensorial_api_crm_list_users, …
#   tools/call runs the skill through the normal client path
```

Tool names come from capabilities; input schemas are derived from each skill's
ingress schema.

**It streams.** A `tools/call` from a client that sends
`Accept: text/event-stream` is answered as one: each chunk a skill produces
becomes an MCP `notifications/progress` message as it is produced, and the
JSON-RPC response closes the stream. A client that does not ask for a stream
still gets the buffered JSON body. This border used to be request/response
only, which made it contradict the argument the rest of the runtime rests
on — a skill that streams token by token should not be flattened into a single
reply at the one boundary an external agent actually uses.

**Its gates can be answered.** An effect invoked over MCP is gated the same as
any other, but the client on that socket is a program, so there is no human on
it to ask. The question is parked in the node's approval queue and answered
from the control-plane UI, `aura approve`, or `POST /v1/approvals/{id}`; the
call blocks until then and denies itself if nobody answers. It previously
resolved as an automatic refusal with a pointer to the UI — safe, but it meant
an external agent could read through this node and never act through it. See
[Guarding an agent's tools](#guarding-an-agents-tools), which is what that
unlocked.

**A2A discovery card** at `/.well-known/agent.json`, built from the live catalog,
so other agents can discover this node and its skills. Discovery only — the A2A
*message* protocol is not implemented, and "A2A support" here should be read as
exactly that one endpoint.

**OpenTelemetry export.** The causal log becomes OTLP traces — one span per
envelope, `parentSpanId` set to the causal parent — so any OTel backend (Jaeger,
Grafana Tempo, …) renders the causal tree of a session:

```powershell
.\kernel\aura.exe trace sess-… --otlp http://localhost:4318   # to a collector
.\kernel\aura.exe trace sess-… --out trace.json               # or to a file
```

---

## Guarding an agent's tools

Everything above assumes you are building *on* this runtime. This does not.

An agent — Claude Code, Cursor, OpenClaw, anything speaking MCP — calls its
tool servers directly. Whatever a tool will do, it does; nothing decides
whether it should, and nothing records that it did. `aura guard` puts this
node's checkpoint in the middle of that path without asking anyone to rewrite
an agent or adopt a runtime:

```
agent ──▶ aura /mcp ──▶ executor ──▶ guard ──▶ the real MCP server
                          │
                    policy · gate · ledger
```

Point it at the `mcpServers` config you already have — the format Claude
Desktop, Cursor and the rest already write, unmodified:

```powershell
.\kernel\aura.exe guard --config .\claude_desktop_config.json --dry-run
```

```
  aura guard — would guard 2 tool(s) on node node-7d6794d2

  TOOL                CAPABILITY                    ON CALL
  ------------------  ----------------------------  ------------------------
  github/delete_repo  motor.mcp.github.delete_repo  human approval + sealed
  github/list_repos   motor.mcp.github.list_repos   human approval + sealed

  2 of 2 act on the world and are gated; the rest are read-only.
```

Drop `--dry-run` to guard for real, then point the agent at this node instead
of at the servers directly:

```powershell
claude mcp add --transport http aura http://localhost:9080/mcp
```

From then on a call into a tool that acts on the world stops and waits:

```powershell
.\kernel\aura.exe approvals
#   01M051C3ZTJQ…
#       Approve delivery to s.call?
#       tool     motor_mcp_github_delete_repo (via mcp)
#       args     {"body":{"repo":"prod-database"}}
.\kernel\aura.exe approve 01M051C3ZTJQ…      # or --deny
```

and the effect is sealed into the ledger like any other, with a receipt that
verifies offline.

**How this is built matters more than what it does.** Guard contains no policy
code, no gate code and no ledger code. It connects to `/ws/skill` — the same
inversion-of-control socket any out-of-process skill uses, holding no kernel
privileges — and registers one skill per upstream tool, typed `motor` or
`sensorial`. From the executor's side there is nothing special about them, so
they inherit the human-approval invariant, the ledger, cancellation and replay
for free. A guard that made its own decisions would be a second checkpoint, and
the argument of this whole runtime is that there is exactly one.

**A tool is gated unless it proves otherwise.** MCP lets a server annotate a
tool `readOnlyHint`. Guard registers everything as `motor` — gated — including
every tool on a server that annotates nothing at all. `--trust-annotations`
honours the hint and leaves those tools ungated. The default is strict in that
direction on purpose: `readOnlyHint` is a claim made by the same server the
call is about, exactly like a C5 attestation is a claim by the skill that made
it. Believing it in order to *skip* a gate would let a server disarm the guard
by lying about itself; believing it in order to *require* one costs a prompt on
a tool that only reads.

**What this does not do, said plainly.** A guard that can be bypassed is
theatre. Nothing here stops an agent from talking to the original MCP server
directly — the guarantee holds exactly as far as your control over the agent's
configuration does. That makes this useful on a machine or in a team where the
config is managed, and decorative where it is not. There is no network
enforcement, no egress control, and no attempt to detect an agent routing
around it.

Two smaller limits worth knowing: an in-flight `tools/call` cannot be
cancelled, because MCP has no cancellation for one — the kernel suppresses
whatever the abandoned chain would have produced, but the upstream server keeps
working. And a gated call holds the agent's request open while it waits, which
some agents will time out on before a human gets there.

---

## The five contracts

Everything above rests on five small, formally specified, version-frozen
contracts in [`spec/`](spec/). They are the *only* thing AURA invents; a
conformance suite ([`spec/conformance/`](spec/conformance/), 59 checks, black-box
over the raw protocol) is how an implementation demonstrates it complies. That
is what lets alternative kernels and SDKs exist without fragmenting the
ecosystem — though 59 checks test C1-C3, not every behaviour a real kernel
needs, and C4/C5 have their own guarantees checked by an adversarial CI job
rather than by that suite (see [Security model](#security-model)).

- **C1 — Manifest** ([spec/c1-manifest.md](spec/c1-manifest.md)): what a skill is
  — identity, type, capability, typed ports with mandatory schemas, declared
  permissions and resource needs, optional compensation metadata, signature.
- **C2 — Graph IR** ([spec/c2-graph-ir.md](spec/c2-graph-ir.md)): the single graph
  format both human YAML and planner output compile to — nodes declaring a
  *capability demand*, edges with optional human-approval gates, parallel waves.
- **C3 — Channel** ([spec/c3-channel.md](spec/c3-channel.md)): the message envelope
  and its normative semantics — FIFO ordering, at-least-once delivery with
  idempotency, mandatory back-pressure, causal `cause_id`, cancellation,
  declarable QoS, negotiated transport, metering, an optional effect receipt,
  and an optional inference attestation.
- **C4 — Effect Ledger & Policy** ([spec/c4-ledger.md](spec/c4-ledger.md)): the
  Effect Checkpoint every `motor.*` delivery passes through — authorize by
  node policy, attest into a hash-chained entry, commit to an RFC 6962 Merkle
  head, periodically sign it, optionally have a third party counter-sign it —
  and the offline verification a conforming implementation must support.
- **C5 — Inference Attestation** ([spec/c5-attestation.md](spec/c5-attestation.md)):
  what a skill asserts about how it produced an output — engine, model,
  revision, quantization, sampling parameters, seed — content-addressed and
  cited by every effect it causally led to. C4 answers *by whose authority*;
  C5 answers *on what basis*. It is careful about its own limits: an
  attestation is an assertion bound unforgeably to its consequences, not proof
  of what executed.

**Interruption is a guarantee, not a request.** A client can abandon a chain
mid-flight — the "stop talking, I changed my mind" of a live conversation. The
kernel marks that chain cancelled and routes nothing further belonging to it,
so a skill that ignores the cancel and keeps generating produces output that
goes nowhere. It also *asks* every skill working on the chain to stop, at any
depth, each addressed with the `cause_id` that skill will recognise — a skill
three hops down never saw the id the client named. Cancelling is scoped to the
one chain: a second utterance, or a vision stream sharing the session, is
untouched.

Frozen means additive-only within a major version; a breaking change requires a
new major and a written justification.

---

## Security model

Security is *designed* to be **progressive** — friction appears exactly where
risk appears, and nowhere else. That is the intent; what follows is how much of
it the code enforces today, because the gap matters more than the intent.

### Authentication — what a default node does

`aura up` with no flags binds **loopback**, generates a **bearer token** on
first start (written `0600`, printed once, reused on restart), requires it on
`/v1/*`, `/ws/*`, `/mcp` and the A2A card, and checks the **WebSocket Origin**
against an allowlist. `--listen 0.0.0.0` is an explicit opt-out that prints a
warning; `--tls-cert`/`--tls-key` terminate TLS; `--no-auth` disables the token
for a single-user loopback node and says so loudly at startup.

Three paths are open without a token, each for a stated reason: `/healthz`
(liveness probes hold no credential), `/hooks/*` (webhook senders authenticate
with their own HMAC instead), and the UI's static assets (a browser must load
the page before it can present anything). The capability catalog is **not**
among them — it names every effect the node can produce.

The origin check is separate from the token on purpose: a browser attaches
credentials to a cross-site WebSocket handshake by itself, so the token alone
would not stop a malicious page driving a node the user is authenticated
against.

### Authorization — the node decides, not the graph

A node loads an authorization policy at startup, hashes it, and prints the hash.
Deny-by-default for `motor.*`; first matching rule wins; no expression language,
because a policy an auditor cannot read by looking at it is not a policy.

```yaml
policy: 1
default_effect: gate
rules:
  - match: "motor.tts.speak"
    decision: allow
    reason: "speech is continuous; per-clause approval is unusable"
  - match: "motor.erp.*"
    decision: gate
    limit: { per_minute: 10 }
  - match: "motor.payments.*"
    decision: deny
```

**A graph may be stricter than policy, never laxer.** A `deny` is absolute. An
edge asking for `human-approval` always gets it. An edge declaring
`"gate": "none"` is a *request*, honoured only by a node that granted graphs
that authority — which is what closes the hole where anyone able to POST a graph
could waive the kernel's strongest guarantee. Running `aura up` without
`--policy` loads a built-in default that is a real hashed document, reproducing
the pre-policy behaviour so the seeded graphs still work.

**A waiver is recorded as one.** Where a policy does grant graphs that
authority, the entry sealed for the effect carries `waived: true` — because
`decision: allow` alone cannot distinguish "the operator's policy cleared this"
from "whoever registered this graph cleared it", and a graph is a JSON document
anyone able to reach the control surface may POST. The credential broker refuses
to spend a receipt carrying it, so a waiver stays a way to lower friction and
does not become a way to mint the authorization that buys a secret. See
[C4](spec/c4-ledger.md) and [The credential broker](#the-credential-broker).

**When the ledger cannot be written.** Sealing writes to a disk, and disks fill
up. `on_seal_failure` decides which of two bad outcomes this node prefers:

```yaml
on_seal_failure: deliver   # default: the effect goes through, the gap is logged
on_seal_failure: refuse    # the effect is stopped rather than left unattested
```

There is no third option where both the node stays up and the ledger stays
complete, so it is the operator's call rather than the kernel's. `deliver` is
the default because a runtime whose premise is that it never stops should not
turn a full disk into an outage; a node sealing payments wants `refuse`. The
startup banner prints which is in force, and an effect that could not be sealed
carries no receipt — so nothing downstream can mistake it for attested.

| Mode | Intended for | What it changes today |
|---|---|---|
| `local` | localhost, one user | The default. Token, origin check and policy all apply. |
| `site` | several nodes of one org | **Nothing beyond `local` yet.** Automatic node identity and encrypted inter-node channels are designed, not implemented. |
| `published` | third-party code / public network | Two things: an *omitted* gate on a `motor.*` edge is refused rather than repaired, and a graph-level waiver is never honoured whatever the policy says. |

Package signing does not depend on the mode: `aura publish` always signs
(Ed25519) and `aura add` always verifies the artifact hash and the signature
before installing. The registry binds a package id to its first publisher's key
and refuses a different one, and refuses republishing a version with different
content — both covered by tests.

### Scoped credentials

A node had exactly one credential. The CLI, the browser UI, a federation bridge
and every skill process presented the same bearer token, so "can reach this node"
and "is the operator of this node" were the same statement.

That made the credential broker's guarantee weaker than it reads. Its argument is
that skipping the Effect Checkpoint gets you a 401 — true against a process
outside the node, and not against a skill, which held the operator's token and
could therefore register a graph waiving its own gate, seal an effect, and redeem
the receipt it had just minted.

```powershell
.\kernel\aura.exe token issue --capability motor.erp.write --label "erp writer"
#  tok-2sNkcJvt  scoped to motor.erp.write
#
#  mGN2mnCbyz6_fkh-Jd9bXcqBib2NSuHPll0swrQqtPE
#
#  This is the only time it is shown — only its hash is stored.
```

A `skill` credential may do three things: connect to `/ws/skill`, register as the
capability it was issued for, and spend a receipt for the credentials that effect
authorized. It cannot register a graph, read the ledger, store a secret, enrol an
approver or host a Wasm module.

Two properties are what make that real rather than nominal:

- **Registration is bound to the issued capability**, exactly and never by
  prefix. Registration is how a process declares what it *is* to the executor,
  and everything keys off that — which policy rule applies, whether an edge into
  it is an effect, which receipts it can spend. A narrow token free to register
  as anything would be a full one with a smaller name.
- **One ordered table decides what each scope may reach**, deny by default, in
  `internal/gateway/scope.go`. The same argument the policy engine makes about
  rules: an authorization model spread across forty handlers exists only as the
  sum of forty decisions, and a route added next year silently defaults to
  whatever its author remembered. Here a new route is closed to skills until
  someone widens the table in a diff a reviewer can see.

`aura token ls` lists what has been issued; `aura token revoke <id>` stops one
working at its next request. Revocation is recorded rather than deleted, for the
same reason an operator revocation is: an effect sealed while the token was valid
stays explicable afterwards.

The operator's own token is unchanged — it is still `node.token` in the data
directory, and a node that issues none behaves exactly as it did before.

**What this does not do.** It does not contain a skill that has already been
handed a credential it legitimately asked for, and it does not isolate the
process — see [Skill isolation](#skill-isolation). What it removes is the
standing operator authority that every skill used to carry merely by being
started.

### Attestation — the effect ledger ([C4](spec/c4-ledger.md))

Authorization decides whether an effect happens. Attestation is the separate
question of whether anyone can *prove*, later, that it did — and under what
authority. Every effect a node's policy authorizes — delivered outright, or
gated and then approved or refused — is sealed into an append-only ledger
entry:

```json
{ "seq": 42, "prev": "sha256:9f2a…", "actor": "acme/motor/erp-writer@1.2.0",
  "capability": "motor.erp.invoice.create", "decision": "gate", "outcome": "delivered",
  "policy": "sha256:7c1e…", "payload_sha256": "b5b3…" }
```

Five things make this evidence rather than logging:

- **Hash-chained.** Each entry cites the hash of the one before it
  (`prev`), computed over the entry's own content. Altering an old entry's
  content changes its hash, which breaks every entry sealed after it — the
  chain either recomputes cleanly end to end or it visibly does not.
- **Committed to a Merkle head.** Alongside the linear chain, entries form an
  RFC 6962 tree — the Certificate Transparency construction, chosen because it
  gives both proof types below against one shape and closes the
  second-preimage attack a naive tree has. Every checkpoint commits to the
  tree head as well as the chain head.
- **Periodically signed.** Every ~100 entries or 60 seconds, whichever comes
  first, the node signs its current head with its own Ed25519 key
  (`identity.Node.Keys`, generated on first start like every other keypair in
  this runtime). This is what a plain hash chain alone cannot give you: an
  attacker with direct database access could edit an old entry *and* repair
  every `prev` pointer after it, leaving a chain that still recomputes
  cleanly — but they cannot forge a signature over the new head without the
  node's private key.
- **Optionally counter-signed by a third party.** See
  [external anchoring](#external-anchoring) — the part self-signing cannot do.
- **Verifiable offline.** `aura verify [--data <dir>]` recomputes the entire
  chain *and* the tree, and checks every checkpoint signature directly against
  the SQLite file — no kernel process involved, and it never touches the
  private key, only the public one. `GET /v1/ledger/verify` runs the identical
  check against a running node, for convenience; the two can never quietly
  disagree, because it is the same function. Tampering either way — editing
  one entry, or editing an entry *and* repairing the chain after it — makes
  both report failure; this is exercised by a CI job that seals a real effect
  through a running node, edits `kernel.db` directly, and checks that both
  paths catch it.

### External anchoring

A node signing its own history establishes that **nobody altered it without
the key**. It does not establish that the key's holder did not: an operator
can rewrite entries and re-sign the result, and the chain recomputes, every
signature verifies, and nothing on disk records that it said something else an
hour ago. Earlier versions of this document described the ledger as evidence
"an auditor can check without trusting the process that produced it", which
overstated it — the auditor still had to trust whoever held the key.

A **witness** closes that with Certificate Transparency's mechanism: before
counter-signing, it verifies an RFC 6962 *consistency proof* — the entries I
already vouched for are still, unchanged and in the same order, a prefix of
these. A node that rewrote entry 3 cannot produce one; there is nothing to
forge, the proof either exists because history really is an extension or it does
not. The resulting property is that **a node can still lie, but not consistently
to two parties over time.**

[Open witnessing](#open-witnessing) covers the mechanism, the public anchor and
`--open-witness`; [Who watches the witness](#who-watches-the-witness) covers what
keeps the witness itself honest. What belongs here is the part that is about
this node's own trustworthiness.

A node whose own key was used to rewrite history still passes its *self*-checks
— chain intact, checkpoint valid — and `aura verify` reports that combination
for what it is:

```
  7 entries · chain intact · 1/1 checkpoint(s) valid · key 2b850090b4d4
  0/1 witness countersignature(s) verify

  NOT SOUND.
  → 1 of 1 witness countersignatures no longer match.
     Note: the chain and every checkpoint verify. That combination —
     internally perfect, externally contradicted — is what a rewrite by the
     holder of this node's own key looks like. Ask the witness directly.
```

And the limit that survives all of it: **countersignatures stored by a node are
stored by that node**, which can drop the inconvenient ones. "0 witnesses" is
not proof none were issued — the authoritative copy is the witness's own
published log. The tooling says "unwitnessed", never "unwitnessed, therefore
fine".

### Portable receipts

Proving something about one effect used to mean handing over the whole SQLite
file: every unrelated effect came with it, and the recipient needed a tool
that spoke the storage layout. In practice the evidence was never shared,
which makes "verifiable" theoretical.

```powershell
.\kernel\aura.exe receipt sha256:9f2a… --out invoice-99.json
.\kernel\aura.exe receipt --verify invoice-99.json      # anyone, anywhere
```

A receipt is one self-contained JSON document: the sealed entry, an inclusion
proof placing it in a tree of a stated size, the signed checkpoint over that
tree, any witness countersignatures, and the C5 attestations the entry cites.
`--verify` opens no database, contacts no node, and needs no key material
beyond what the document carries. An inclusion proof is ~log₂(n) sibling
hashes, so a receipt discloses the effect it is about and nothing whatsoever
about the ledger's other entries.

### Attestation of the inference itself ([C5](spec/c5-attestation.md))

C4 records *what acted and who authorized it*. C5 records **what argued for
it**: which model, which revision, which quantization, which sampling
parameters, which seed.

```json
{ "engine": "llama.cpp", "model": "Qwen/Qwen2.5-1.5B-Instruct-GGUF",
  "model_revision": "f1d2d2f9…", "quantization": "Q4_K_M",
  "params": { "temperature": 0.7, "seed": 42 },
  "prompt_sha256": "b221…", "energy": { "millijoules": 4120.5, "source": "nvml" } }
```

A skill attaches one to any envelope it emits (`ctx.emit(..., attest=...)` in
the Python SDK). The kernel content-addresses it, stores it once, and cites
its hash in the ledger entry of **every effect that output causally led to** —
so an entry answers "on what basis did this happen", and a silent model swap
changes hashes that are already sealed into an append-only chain.

**What this proves, stated plainly, because the overclaim is tempting:** an
attestation is an *assertion by the skill*, bound unforgeably to what it
caused and to when it was made. It is **not** proof the skill told the truth.
A skill that lies about its model produces an unalterable record of a lie.
What is guaranteed is that the claim cannot be edited afterwards, cannot be
detached from its consequences, and cannot be backdated. Closing the rest
needs hardware attestation (a TEE quote); the `tee` field is reserved for it
and is empty on every node today.

Two related safeties ship with it. Weight downloads resolve and **pin the
Hugging Face revision before fetching**, so `model_revision` names the bytes
actually retrieved rather than whatever `main` pointed at afterwards. And
pickle-based weight formats (`.bin`, `.pt`, `.ckpt`) are **refused** rather
than warned about — they execute arbitrary code on load, and the two formats
this runtime actually uses (GGUF, safetensors) are both pure data.

### Bill of materials

```powershell
.\kernel\aura.exe bom sess-8f3a --out inventory.json
#  CycloneDX 1.6 ML-BOM · 2 skill component(s), 1 model component(s)
```

Every AI-SBOM tool builds its inventory from a manifest or a lockfile — a
statement of what was *supposed* to run. `aura bom` builds it from the ledger:
what actually ran, cited by which effects, under which policy. A model that
was configured but never invoked does not appear; one swapped in at runtime
does. Output is CycloneDX 1.6 with `machine-learning-model` components, so it
drops into tooling that already exists — relevant to the EU AI Act's
record-keeping obligations, in force since 2 August 2026. It inherits C5's
standing exactly: an accurate record of what was *claimed* and what it caused.

The payload itself is never stored — only `payload_sha256` — so the ledger
stays small (~300–400 bytes/entry) and a payload carrying personal data does
not become permanently undeletable. `GET /v1/ledger` lists recent entries
(paginated, behind the token like everything else); `/healthz` includes a
one-line summary (entry count, checkpoint count, node public key) cheap enough
to compute on every probe.

### What is still not enforced

**A `format: source` skill is not contained.** By default it runs with the
privileges of whoever started the node, and `permissions` is a declaration the
registry shows you at install time. `--sandbox process` narrows that — a
scrubbed environment, a working-directory jail, no inherited handles, a
declared-egress check before launch — which stops accidental credential
inheritance and casual filesystem wandering, and stops hostile code not at all:
a determined process still reaches the network and the filesystem through
syscalls no portable Go runtime can intercept. `format: wasm` *is* genuinely
enforced, in a real WASI sandbox. A real boundary for source skills means a
microVM, which is declared and refused at startup rather than stubbed. See
[Skill isolation](#skill-isolation). Until then, a source skill you install is
code you are choosing to run.

**The motor-gate invariant.** Every edge that delivers into a `motor.*` skill
carries whatever gate the node's policy decided. The executor applies it when it
instantiates the session, not the tool that produced the graph, so it is
identically true for a hand-declared graph, planner output, an MCP-generated
graph, or a graph editor that does not exist yet — enforced in
[`kernel/internal/executor/session.go`](kernel/internal/executor/session.go) and
[`policy.go`](kernel/internal/executor/policy.go), and covered by tests including
an adversarial CI job that runs a real binary with default flags and checks it
refuses what it should.

---

## HTTP & WebSocket API

A node serves everything on one port (default 9080).

**Kernel — HTTP**

| Method & path | Purpose |
|---|---|
| `GET /` | The control plane UI. |
| `GET /healthz` | Node identity, mode, protocol/IR majors, admission usage. |
| `GET /v1/skills` | The live capability catalog. |
| `GET /v1/skills/config?id=<id>` | A skill's declared config schema plus its current effective values. |
| `PUT /v1/skills/config?id=<id>` | Set runtime config overrides; validated, persisted, pushed to the live skill. |
| `GET /v1/graphs` · `GET /v1/graphs/{id}` | List / fetch registered graphs. |
| `POST /v1/graphs` | Register a C2 IR document. |
| `GET /v1/sessions` · `GET /v1/sessions/{id}` | List / summarize sessions. |
| `GET /v1/sessions/{id}/events` | A session's causal event log (with timestamps). |
| `POST /v1/projections` · `GET /v1/projections` | Connect / list projections. |
| `POST /v1/projections/{name}/promote` | Change an operation's mode. |
| `POST /v1/ingress` · `GET /v1/ingress` | Declare / list inbound webhook routes. |
| `DELETE /v1/ingress/{name}` | Revoke an inbound route. |
| `GET /v1/approvals` | Human-approval gates currently waiting, with the tool and arguments that raised them. |
| `POST /v1/approvals/{id}` | Answer one: `{"approve": true\|false}`. The field is required — an omitted decision is a `400`, never a guess. |
| `GET /v1/schemas` | Every schema reference this node can compile. |
| `GET /v1/schemas/{ref}` | One schema document (`/v1/schemas/std/text@1`). |
| `GET /v1/grammars/{ref}` | The GBNF decoding grammar for that schema, as `text/plain`. |
| `GET /v1/ledger?from_seq=&limit=` | Paginated effect-ledger entries ([C4](spec/c4-ledger.md)). |
| `GET /v1/ledger/verify` | Recompute the chain, the Merkle tree and every signature; `200` if sound, `409` if not. |
| `GET /v1/ledger/head` | Current position: entries sealed, chain head, Merkle head. |
| `GET /v1/ledger/statement?from_seq=` | What this node presents to a witness: signed head + consistency proof. |
| `POST /v1/ledger/witness` | This node acting as a witness for another. Returns a countersignature, or `409` with the reason it refused. |
| `GET /v1/ledger/witness/last-seen?node=` | How far this node has already vouched for another. |
| `POST /v1/ledger/witness/record` | Record a countersignature about this node's own ledger (re-verified before storing). |
| `GET /v1/sessions/{id}/bundle` | The session's audit bundle. |
| `GET /openenv/spec` · `POST /openenv/reset` · `POST /openenv/step` | OpenEnv environment surface — every registered graph is an environment. |
| `GET /openenv/state` · `GET /openenv/bundle?episode=` | Episode metadata, and the episode's audit bundle. |
| `GET /v1/ledger/receipt/{hash}` | The portable evidence document for one sealed effect. |
| `GET /v1/ledger/attestations/{hash}` | One C5 inference attestation, re-checked against its content address. |
| `POST /hooks/{name}` | Receive a webhook: HMAC-verified, opens its own session. |
| `GET /.well-known/agent.json` | A2A discovery card. |
| `POST /mcp` | MCP Streamable HTTP endpoint (JSON-RPC). |

**Kernel — WebSocket**

| Path | Purpose |
|---|---|
| `GET /ws/skill` | A skill connects here (first frame: a C3 `register` envelope). |
| `GET /v1/stream?graph=<id>[&session=<id>]` | A client opens a session against a graph. |

Clients may send a full C3 envelope, or the shorthand `{"text": "…"}` — handy for
`curl` or a browser without the SDK.

**Registry — HTTP** (served by `aura registry serve`, default port 9091)

| Method & path | Purpose |
|---|---|
| `GET /r1/health` | Registry liveness. |
| `POST /r1/packages` | Publish a signed package. |
| `GET /r1/packages[?capability=<cap>]` | List / discover packages. |
| `GET /r1/packages/{org}/{cat}/{name}[/{version}]` | Package metadata. |
| `GET /r1/packages/{org}/{cat}/{name}/{version}/artifact` | Download the artifact. |

---

## Repository layout

```
README.md           The short front door: what it is, the 60-second start.
GUIDE.md            This document — the complete reference.
README-ES.md        Spanish front door.
GUIDE-ES.md         Spanish translation of this document.
CONTRIBUTING.md     Contributing to the kernel, SDK, and spec themselves
                    (to add a skill instead, copy skills/echo/ — the minimal
                    worked example — or any other skill under skills/).
LICENSE.md          Component-by-component license map, commercial licensing.
LICENSE-APACHE-2.0.txt   Full Apache-2.0 text (spec, SDK, docs).
LICENSE-AGPL-3.0.txt     Full AGPLv3 text (kernel, UI, first-party skills).
spec/               The five frozen contracts + JSON Schemas + conformance suite.
  c1-manifest.md      C1 — skill manifest.
  c2-graph-ir.md      C2 — the single graph IR.
  c3-channel.md       C3 — the channel protocol.
  c4-ledger.md        C4 — effect ledger, Merkle head, witnessing, receipts.
  c5-attestation.md   C5 — inference attestation, and what it does not prove.
  enums.yaml          Single source of truth for every enum C1-C5 use; generates
                      the Go/Python/TypeScript constants (scripts/gen_ssot.py).
  schemas/            Executable JSON Schemas for C1-C3 and the C5 attestation
                      (C4 has no wire schema to validate — see c4-ledger.md).
  conformance/        Black-box suite (runner.py) + reusable test vectors.
kernel/             The Go kernel — the single `aura` binary.
  cmd/aura/           CLI commands (up, chat, do, connect, publish, why, federate,
                      verify, guard, approve, …).
  internal/           identity · channel · registry · executor · store · gateway
                      · ledger (effect ledger C4 + Merkle tree, witnessing,
                      portable receipts, C5 attestations, ML-BOM) · projection · signing
                      · hub (registry) · mcpsrv (skills as MCP tools) · mcpcli
                      (MCP client: the servers someone else runs) · guard
                      (an agent's tool calls, put behind the checkpoint) ·
                      approvals (where a gate finds a human) · fed (federation)
                      · spec (generated constants) · config (the --config file
                      layer, see "Runtime skill config").
ui/                 Control plane frontend (React + Vite + TypeScript, i18n).
sdk/python/         The `aura` SDK for writing skills (includes aura.llm.ChatBackend).
sdk/node/           @deepaxiom/aura — expose an app's functions as skills, or
                    drive a graph from TypeScript. Types generated from spec/schemas.
skills/             Reference skills — worked examples carrying the `example/`
                    org, not a catalogue. One per shape: echo (the minimal
                    one), llm-chat, asr, tts, sentence-chunker, planner,
                    model-manager, memory-context, postgres-cdc. See
                    skills/README.md.
scripts/            release.ps1 (distributable zip), gen_ssot.py (spec/ → generated
                    constants), check_links.py, adversarial.sh and
                    ledger_adversarial.py (CI: a real binary refuses what it should).
aura-landing/       The marketing site (Astro) — independent from the runtime.
```

---

## Build & release

```powershell
# Rebuild the kernel after Go changes:
cd kernel; go build -o aura.exe ./cmd/aura

# Rebuild the UI, then re-embed by rebuilding the kernel:
cd ui; npm run build; cd ..\kernel; go build -o aura.exe ./cmd/aura

# Run the conformance suite against a running node:
pip install websockets jsonschema
python spec\conformance\runner.py --port 9080

# Package a distributable release (binary with UI embedded + SDK + skills + spec):
.\scripts\release.ps1 -Version 0.1.0
#   → release\aura-0.1.0-windows-amd64.zip  (~10 MB)  →  unzip, then .\aura\aura.exe up
```

---

## Milestone status

**This is pre-1.0.** See [Milestone status](#milestone-status) for what is built and what
is not — the foundations, the connectivity work, streaming multi-channel voice,
node security and the effect ledger are all in, browser client included. The
eighteen milestones below run end-to-end; the conformance suite (59 checks,
`spec/conformance/`) covers C1-C3, not the milestones, and the effect ledger's
own guarantees are checked separately by an adversarial CI job (see [Security
model](#security-model)). A `go test` suite covers part of the kernel — see
[test coverage](#partial-test-coverage) below for which part — and CI blocks a
merge that breaks the build, the tests, the race detector, the formatting, the
embedded UI bundle, the conformance suite, the SSOT check, or a documentation
link.

"Tested" below means covered by automated tests in CI. "Hand-verified" means it
was exercised manually and works, but nothing stops a regression.

| Milestone | What it delivered | Verification |
|---|---|---|
| Single-binary kernel + SDK + distro | `aura up` → UI + local LLM chat, no external services | Tested |
| Spec + conformance | Contracts frozen — C1 v1.5, C2 v1.2, C3 v1.6, C4 v1.2, C5 v1.0; 59-check black-box suite | Tested |
| Streaming multi-channel voice | `voice` graph + browser client: partial transcripts, spoken replies, working barge-in | Kernel tested; client hand-verified |
| Connectivity without OpenAPI | Declarative connector pattern, TypeScript SDK, webhook ingress, traffic observation | Ingress + generator tested; connector pattern hand-verified (no first-party connector skill is bundled) |
| Model drivers + admission | ASR / TTS skills; `--memory-budget` | Admission + ASR/chunker tested; drivers hand-verified |
| Planner + `aura do` | Natural-language goal → plan → gated execution | Hand-verified |
| Explainability + replay | `aura why` (LLM-narrated), `aura replay` | Hand-verified |
| Legacy OpenAPI projection | `aura connect` → operations as skills, four safeties | Tested (`projection` 61%) |
| Federable registry | `aura publish/add/run`, Ed25519 + trust-on-first-use | Tested (`signing` 90%, `hub` 81%) |
| Standard borders | MCP server, A2A card, OpenTelemetry export | MCP tested (63%); OTel export hand-verified |
| Node federation | `aura federate`, resolution across nodes | Tested (`fed` 87%) |
| Negotiated transport, LAN/same-host (Phase 3) | One pooled connection per federated capability instead of one dial per envelope; cancel-by-envelope, not socket-close; measured route classification (`aura federate` prints it) | Tested — connection reuse, concurrent-relay demux, cancel-doesn't-affect-a-peer, self-heal after a drop |
| Effect ledger (C4) | Hash-chained, signed attestation of every effect; `aura verify`, `GET /v1/ledger[/verify]` | Tested (`ledger` 89%); tamper-detection proven by a CI job driving a real binary |
| Merkle head + portable receipts (C4 v1.2, Phase 4) | RFC 6962 tree over every entry, committed in each checkpoint; `aura receipt` exports one effect's evidence and `--verify` checks it with no database, node or network | Tested — inclusion proofs at every position for tree sizes 1-64, consistency proofs for every (m,n) pair to 48, and negative cases including an entry rewritten with a repaired hash |
| External witnessing (C4 v1.2, Phase 4) | `aura witness <peer>`: a third party verifies a consistency proof before counter-signing, so a rewritten history cannot be passed off to someone who already saw the old one. Every node is a witness | Tested — honest extension accepted, forked history refused, countersignature bound to its node, HTTP handshake between two nodes |
| Inference attestation (C5, Phase 4) | A skill declares engine/model/revision/quantization/sampling/seed; the kernel content-addresses it and cites it in every effect the output causally caused. Pinned HF revisions, pickle weight formats refused, energy reported with its source | Tested — end-to-end through the real executor path (delivered, denied, malformed, and no-inference cases), plus receipt round-trip |
| ML-BOM (Phase 4) | `aura bom` emits CycloneDX 1.6 from the ledger — models and skills that actually ran, not configured | Hand-verified |
| Typed ports enforced (Phase 5) | A GBNF grammar compiled from each port's declared schema, pushed to the skill at registration and served at `/v1/grammars/{ref}`. The validation half is written but **not wired** — a payload is not checked against its port's schema on the way past today, and `deadcode` is what surfaced that | Tested — the grammar suite runs a GBNF evaluator over generated grammars and asserts they accept every valid document and reject invalid ones, including the all-optional-properties case that permits `{,"b":1}` |
| Speculative graph execution (C2 v1.2, Phase 5) | `speculative: true` on an edge runs downstream work on partial output and discards it on divergence; **refused at wiring on any edge into a `motor` skill**, which makes the safety question a type-system answer rather than a heuristic | Tested — motor refusal, hit suppression, miss cancel-and-redeliver, schema-directed accumulation, policy opt-out |
| Deadlines, priority, context budget (C2 v1.2, Phase 5) | Absolute inheritable deadlines a hop may tighten but never extend; preemption priority; a per-graph context budget the executor enforces | Tested |
| Audit bundles (Phase 6) | `aura bundle` — trajectory, a standalone receipt per sealed effect, and the model configurations behind them, in one document that verifies offline. The four materials [arXiv 2607.22368](https://arxiv.org/abs/2607.22368) identifies as the difference between an auditable score and a number | Tested — edited, dropped and reordered steps all fail; truncation is declared, not hidden; verified end to end against a live node |
| OpenEnv border (Phase 6) | Every registered graph is a Hugging Face OpenEnv environment; the episode returns its audit bundle alongside the observation. Reward is always null, deliberately | Hand-verified end to end (reset → step → bundle → offline verify) against a live node |
| Skill isolation (Phase 6) | `--sandbox process`: the environment becomes an allowlist instead of an inheritance, plus a cwd jail. `microvm` is declared and **refused at startup**, not stubbed | Tested — credential scrubbing, explicit passthrough, microvm refusal, wasm/format agreement |
| Open witnessing (Phase 6) | `--open-witness` lets nodes outside the trust domain anchor here, bounded by rate, capacity and retention | Hand-verified; bounds unit-covered |
| Routing by policy (Phase 5) | `routes:` in the node policy pins which package answers a capability, so "which model answered" is readable in the signed document an auditor already reads | Tested |
| Reversibility (`aura undo`, Phase 2) | Undo an effect via its declared `compensates` port — itself a gated, sealed effect; refused before a session is built if already undone, never delivered, or irreversible | Tested (adversarial: double-undo, no-compensation, denied-gate cases) |
| Session resume (Phase 2) | A session's dedup window, causal/in-flight indexes, pending gates and per-hop `Seq` counters are reconstructed from the causal event log on reconnect — whether the client dropped or the kernel process restarted, both take the same path | Tested (adversarial: pending gate survives, cancel still reaches an in-flight multi-hop chain, `Seq` continues rather than resetting) |
| Deterministic replay (Phase 2) | `aura replay` diffs the original and replayed sessions' sealed ledger entries, not just the client-visible transcript — closes the thesis's fourth property (Reproducible) | Tested (`ledger.Diff`, pure, 8 table-driven cases) |
| Concurrency: group commit, batched sessions, replica spread | Every envelope is still durably logged before it is acknowledged; what changed is that callers already waiting join one transaction. Session bookkeeping joined the same batch, and resolution now rotates across replicas of a package instead of always answering from the newest connection | Measured, not asserted: 200 concurrent sessions went from 344 msg/s at p50 489 ms to 1,908 at p50 63 ms, and 1,000 sessions (16 replicas) reach 4,215 msg/s at p50 65 ms. Throughput now rises with load instead of collapsing. Reproduce with `kernel/cmd/loadgen/` |
| Session fault isolation | A panic while routing fails that one session and is logged with its stack, instead of unwinding to the top of the goroutine and taking the node — every other session, connection and the ledger's in-memory chain state — with it | Tested — a skill that panics on delivery fails its own session, the error is reported rather than swallowed, and the node keeps starting and routing new sessions |
| QUIC / WebTransport transport (C3 QoS, made real) | The three QoS classes become three transport primitives: `realtime` a QUIC datagram (or its own stream when oversized), `reliable` one ordered stream, `bulk` a stream per transfer. Served on the same port number as TCP; a peer that cannot reach UDP keeps the WebSocket path | Tested against a real QUIC client, not mocked — FIFO on the reliable lane, a realtime frame arriving as an actual datagram, oversized-frame fallback, `bulk` not delaying a terminal envelope, a producer that never blocks, and a skill registering over WebTransport into the real gateway (`wtsrv` 78.5%) |
| Wasm skills, real sandbox (Phase 3) | `format: wasm` on wazero — `filesystem` (WASI preopen) and `egress_http` (custom `env.http_fetch` host import, exact-hostname allowlist) both enforced, not printed declarations; hosted in-process, never a separate OS process | Tested against real compiled `GOOS=wasip1` guests, not mocked (`wasmrt` + `gateway` end-to-end; concurrency-tested permission scoping) |
| Postgres CDC (Phase 3) | `skills/postgres-cdc` — logical replication (`test_decoding`, no extension install) becomes `std/db-change@1` causal events, one per row change | Tested against a real Docker `postgres:16`, not mocked — INSERT/UPDATE/DELETE, NULL round-trip, table filter |
| MCP client + streaming border | `internal/mcpcli` — an MCP client over stdio and Streamable HTTP; and `tools/call` on the server side now answers an event stream, so a skill's output reaches an agent as it is produced | Tested — stdio against a real subprocess (concurrent demux, cancellation, server exit), SSE unwrapping, pagination, and the streaming border end to end (`mcpcli` 89.6%, `mcpsrv` 83.2%) |
| Answerable gates | A human-approval gate raised by a programmatic client is parked in a node-level queue and answered from the UI, `aura approve`, or `POST /v1/approvals/{id}`; unanswered, it denies itself | Tested — approve, deny, expiry-denies-rather-than-hangs, double-resolve, and the no-queue fallback that still refuses (`approvals` 100%) |
| `aura guard` (MCP tools behind the checkpoint) | The MCP servers an agent already uses are registered as skills — `motor` unless the operator opts into trusting `readOnlyHint` — so every tool call passes policy, gate and ledger without guard implementing any of them | Tested end to end against a real gateway and executor: the upstream server is **not** called before approval, **is** after one, a denial never reaches it, and untrusted annotations still gate (`guard` 80.9%) |
| Event-log fsync on the delivery path | `AppendEvent` is one transaction per envelope on the hot path; SQLite's default `synchronous=FULL` made every streamed token pay an fsync. Now `NORMAL` (overridable with `AURA_SQLITE_SYNCHRONOUS`) | Measured, not asserted: 82 → 1,584 envelope round-trips/sec through the real gateway and executor, p50 12.0 ms → 0.42 ms on one developer machine. The change also exposed a latent boundary bug in the ingress dedup window, fixed with it |

### What "streaming and persistent" does and doesn't mean yet

Both words are true of some of the stack and not of the rest. Where the line
falls:

**Holds up today:**
- Every session keeps a durable, append-only causal event log in embedded
  SQLite — history survives a restart, and `aura why` / `aura replay` read it
  back. This is audit persistence: the record of what happened survives, the
  live session does not.
- The wire primitive is a typed, causal, back-pressured stream (C3), not a
  request/response call — text/audio/events all flow as the same envelope
  kind, ordered and idempotent.
- **Connections are kept honest.** Both long-lived sockets (`/ws/skill`,
  `/v1/stream`) ping every 20s and drop a peer after 60s of silence, so a
  half-dead TCP connection — a slept laptop, a dropped wifi, a SIGKILLed
  process — releases its session, its admission reservation and its registry
  entry instead of leaking them for the life of the node.
- **Per-edge QoS is enforced, not just declared.** A `reliable` edge blocks its
  producer until the receiver catches up; a `realtime` edge never blocks and
  discards the *oldest* frame instead, because in a live stream the stale frame
  is the worthless one. Terminal envelopes (`done`, `error`, an approval
  request) always travel reliably whatever the edge says. On a `realtime` edge
  the event log keeps the envelope and its causal links but replaces the
  payload with a description of it — a minute of speech is megabytes of base64
  and a permanent recording of someone talking, and neither `aura why` nor
  `aura replay` needs the samples to do their job.
- **Session resume.** Reconnecting a client with the same session id
  reconstructs the dedup window, causal/in-flight indexes, pending gates and
  per-hop `Seq` counters from the causal event log — whether the client
  dropped or the kernel process restarted, both take the same path. See
  [Milestone status](#milestone-status).

**Not yet true:**
- **`bulk` QoS is still undefined.** `reliable` and `realtime` are specified
  and enforced (see above); `bulk` is a name in the enum with no behaviour
  behind it, and is treated as `reliable` so nothing is silently lost against
  an unspecified rule.
- **One session, one socket, one node.** There's no multi-device view of the
  same live session (the "one guild, many connected clients" shape Discord
  has), and no failover if the owning node goes down mid-session — only the
  event log survives that; the live routing state does not.
- <a id="partial-test-coverage"></a>**Partial test coverage, concentrated in
  the core.** Measured by `go test ./... -cover`:

  | Package | Coverage | | Package | Coverage |
  |---|---|---|---|---|
  | `approvals` | 100.0% | | `guard` | 80.9% |
  | `config` | 100.0% | | `wtsrv` | 79.7% |
  | `channel` | 97.5% | | `registry` | 78.8% |
  | `signing` | 90.4% | | `identity` | 78.3% |
  | `approver` | 90.0% | | `mcpsrv` | 75.6% |
  | `sandbox` | 89.7% | | `ledger` | 73.7% |
  | `mcpcli` | 89.6% | | `projection` | 58.6% |
  | `fed` | 86.4% | | `gateway` | 56.4% |
  | `broker` | 86.2% | | `store` | 56.0% |
  | `wasmrt` | 85.6% | | `grammar` | 52.3% |
  | `executor` | 83.7% | | `cmd/aura` | **7.5%** |
  | `seglog` | 83.7% | | | |
  | `hub` | 81.2% | | | |

  No package is at zero. `internal/` overall is at **72.2%**; the whole-module
  figure (`go test ./...`, all packages) is **53.9%**, and the gap between the
  two is `cmd/aura`: roughly 3,200 lines of CLI at 7.5%, exercised by running
  real nodes end to end rather than by unit tests — `aura verify`'s core logic
  is the exception, tested directly (see [Security model](#security-model)).

  These are re-measured on every release rather than carried forward, because
  this table has drifted before and the drift always ran the same direction: a
  package grew, its tests did not, and the old number kept being quoted. Two
  still sit lower than a reader might expect. `gateway` (56.4%) and `store`
  (56.0%) both carry large accessor surfaces whose error paths are untested —
  the guarantees they implement are covered thoroughly, the plumbing around
  them is not. `cmd/aura` is the honest outlier and always has been: it is
  argument parsing and output formatting over logic that is tested where it
  lives.

  The UI has no test suite at all. The conformance suite exercises the kernel
  end to end over the wire; the effect ledger's tamper-detection guarantee is
  exercised separately, against a real binary, by a dedicated adversarial CI
  job. Treat this as pre-production.

---

## Designed, not yet built

Honest scope. All of these rest on the same frozen contracts — none of them
changes C1, C2, C3, C4 or C5 — but are not yet implemented. Skill sandboxing
is the one that keeps this from being safe outside a trusted network:

- ~~**Node authentication and a loopback default**~~ — **done.** A node binds
  loopback, generates a bearer token, checks the WebSocket origin and can
  terminate TLS; a node policy decides what may act on the world and a graph
  cannot waive it. See [Security model](#security-model) and
  [Milestone status](#milestone-status).
- **A microVM sandbox for `format: source` skills** — the largest remaining
  security gap, now narrowed rather than open. `--sandbox process` makes the
  environment an allowlist instead of an inheritance and jails the working
  directory, which closes accidental credential leakage; `format: wasm` is
  genuinely enforced. Neither contains hostile code. A real boundary means a
  microVM (Firecracker, Cloud Hypervisor, Kata) or gVisor, which needs Linux
  with KVM — the backend is declared and **refused at startup** rather than
  stubbed, because a silent downgrade would be worse than an absence. See
  [Skill isolation](#skill-isolation).
- **What `site` and `published` mode are supposed to do** — automatic node
  identity, encrypted inter-node channels and permissions evaluated at runtime.
  Today the mode string changes two behaviours in `published` — an omitted gate
  on a `motor.*` edge is refused rather than repaired, and a graph-level waiver
  is never honoured — while `site` still behaves exactly like `local`. The table
  in [Security model](#security-model) says so.
- **Hardware attestation (TEE)** — the gap that keeps a C5 attestation an
  *assertion* rather than proof. A quote from Intel TDX, AMD SEV-SNP or
  NVIDIA Confidential Computing, binding a measurement of the process that
  actually ran the model, would close it. The record already has a `tee`
  field, so landing it is additive; what is missing is the integration and
  the hardware to test it on. Until then the documentation says "asserted",
  never "proven" — see [Security model](#security-model).
- **A visual graph editor** — today a graph is authored as raw C2 IR JSON or
  generated by the planner; the control-plane UI's Graphs view is a read-only
  JSON viewer, not a drag-and-drop canvas.

- **Embedded library (`libaura`)** — the same channel client as a linkable
  C/Rust library with Kotlin/Swift/JS bindings, so a TV/mobile/watch app captures
  and plays streams and runs light logic without a full node. A packaging effort;
  the protocol is already the contract.
- **Wasm skills** — a `format: wasm` executor (wazero) for hard-sandboxed logic
  running in-process. The manifest already carries `format`; source/Wasm parity is
  a settled trade-off.
- **Peripheral protocol** — a minimal MQTT/BLE bridge that projects sensors and
  controls (which run no logic) as graph ports — structurally identical to the
  OpenAPI projection host.
- **More borders** — importing external MCP servers as skills, the full A2A
  message protocol, and AG-UI toward frontends.
- **Managed cloud** — a hosted, multi-tenant deployment. Deliberately last: the
  cloud is just another node, and the open runtime must be complete first.

---

## License & governance

**Deep Axiom is the umbrella brand; AURA names the kernel architecture** — the
contracts and execution model that `kernel/` implements. The two are licensed
differently on purpose, in full in [`LICENSE.md`](LICENSE.md):

- **Apache-2.0** for `spec/`, `sdk/`, `aura-landing/`, `scripts/`,
  and this documentation — neutral and open forever. Anyone can build a
  conformant kernel, SDK, or skill against the standard with zero friction and
  zero obligation; the ecosystem has to stay safe for commercial skill authors
  for the marketplace to mean anything.
- **AGPLv3, or a commercial license,** for `kernel/`, `ui/`, and the first-party
  `skills/` — the product itself. Self-hosting, modifying, and auditing are
  unrestricted; offering a modified copy as a network service requires either
  publishing your modifications (AGPLv3) or a commercial agreement. Nonprofits
  and foundations can request the commercial license at no cost or a reduced
  rate — see [`LICENSE.md`](LICENSE.md) for how.

Because the registry is federable and the contracts are conformance-tested, no
single steward can capture the *standard* — forking the spec and SDKs is always
trivial. The kernel's copyleft exists for the narrower, different problem of a
cloud provider hosting the product itself without ever contributing back.

*Everything described in this README has been run; nothing here is a plan
dressed as a feature — what is only designed lives in its own section. It is
pre-1.0: `cmd/aura` is thinly covered by unit tests (exercised instead by
running real nodes), there is no visual graph editor, and the integration
catalog is small. [Milestone
status](#milestone-status) is the accurate summary; if it and this document
ever disagree, Milestone status is right.*

---

## Reference

The shape of [Audit bundles](#audit-bundles) — and the reason this runtime
emits one at all — comes from:

> **Do Agent Benchmarks Measure Capability? Protocol Validity in the Age of
> Agentic AI** · [arXiv:2607.22368](https://arxiv.org/abs/2607.22368) (2026)
>
> Examined published agent traces and found 67% contained *protocol
> exposures* — paths by which a score could be earned without the measured
> capability. Concludes that reports should ship the evidence needed to
> interpret them, and names the four materials a runtime must emit: complete
> trajectory logs, artifact provenance with hashes, replayable model
> configuration, and comparison baselines.

`aura bundle` emits those four. The correspondence was found after the fact —
the causal log, the effect ledger and the inference attestation were each
built for unrelated reasons — which is why the bundle is an assembly of
existing parts rather than a new mechanism.
